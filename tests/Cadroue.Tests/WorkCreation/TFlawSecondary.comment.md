# TFlawSecondary.cs

## Inline notes

### `Assert.Null(TInterface.TFlawSecondaryResolve(`

The secondary-only pass over a file with no subtitle/data output stream makes the null muxer complain.
That carries no secondary defect.
