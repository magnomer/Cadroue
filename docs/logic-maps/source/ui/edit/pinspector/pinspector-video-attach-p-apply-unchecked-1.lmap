@format 1
@id ui-event.6b8f60c321a05b2a
@title Apply — unchecked
@section I. UI
@area UI event
@tabs Edit
@event-ref cs|PPanels/PInspectorVideo.cs|PInspectorVideoAttach|pApply|Unchecked|1
@entry input
@summary Handle the unchecked event exposed by PInspector.PInspectorVideoAttach and follow its registered code path.
@tag unchecked
@tag PInspector
@alias owner = Cadroue.UIShell.PPanels.PInspector

[input] UI event <input>
- Receive the unchecked event from the apply @ owner.PInspectorVideoAttach
> route

[route] Registered handler path <process>
- Update tone apply @ owner.PToneApplyUpdate
- Update the visual or selection state owned by the handler @ owner.PToneApplyUpdate
> output

[output] UI state after the event <output>
= The unchecked event has completed through the current implementation.
! Code binding: cs|PPanels/PInspectorVideo.cs|PInspectorVideoAttach|pApply|Unchecked|1
