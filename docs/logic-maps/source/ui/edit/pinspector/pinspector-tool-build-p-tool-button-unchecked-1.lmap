@format 1
@id ui-event.99beb31eb1ba89eb
@title Tool button — unchecked
@section I. UI
@area UI event
@tabs Edit
@event-ref cs|PPanels/PInspectorCropUi.cs|PInspectorToolBuild|pToolButton|Unchecked|1
@entry input
@summary Handle the unchecked event exposed by PInspector.PInspectorToolBuild and follow its registered code path.
@tag unchecked
@tag PInspector
@alias owner = Cadroue.UIShell.PPanels.PInspector

[input] UI event <input>
- Receive the unchecked event from the tool button @ owner.PInspectorToolBuild
> route

[route] Registered handler path <process>
- Clear inspector crop @ owner.PInspectorCropClear
- Evaluate the handler's implemented conditional branches @ owner.PInspectorCropClear
> output

[output] UI state after the event <output>
= The unchecked event has completed through the current implementation.
! Code binding: cs|PPanels/PInspectorCropUi.cs|PInspectorToolBuild|pToolButton|Unchecked|1
