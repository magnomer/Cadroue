@format 1
@id ui-event.acd62dd00a7972f7
@title Skip apply box — checked
@section I. UI
@area UI event
@tabs Edit
@event-ref cs|PPanels/PInspectorSkip.cs|PSkipBodyBuild|pSkipApplyBox|Checked|1
@entry input
@summary Handle the checked event exposed by PInspector.PSkipBodyBuild and follow its registered code path.
@tag checked
@tag PInspector
@alias owner = Cadroue.UIShell.PPanels.PInspector

[input] UI event <input>
- Receive the checked event from the skip apply box @ owner.PSkipBodyBuild
> route

[route] Registered handler path <process>
- Update the visual or selection state owned by the handler @ owner.PSkipBodyBuild
> output

[output] UI state after the event <output>
= The checked event has completed through the current implementation.
! Code binding: cs|PPanels/PInspectorSkip.cs|PSkipBodyBuild|pSkipApplyBox|Checked|1
