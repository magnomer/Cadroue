@format 1
@id ui-event.b079726d83ba5e81
@title Persistent box — unchecked
@section I. UI
@area UI event
@tabs Edit
@event-ref cs|PPanels/PInspectorCropUi.cs|PInspectorPersistentPrepare|pPersistentBox|Unchecked|1
@entry input
@summary Handle the unchecked event exposed by PInspector.PInspectorPersistentPrepare and follow its registered code path.
@tag unchecked
@tag PInspector
@alias owner = Cadroue.UIShell.PPanels.PInspector

[input] UI event <input>
- Receive the unchecked event from the persistent box @ owner.PInspectorPersistentPrepare
> route

[route] Registered handler path <process>
- Update the visual or selection state owned by the handler @ owner.PInspectorPersistentPrepare
> output

[output] UI state after the event <output>
= The unchecked event has completed through the current implementation.
! Code binding: cs|PPanels/PInspectorCropUi.cs|PInspectorPersistentPrepare|pPersistentBox|Unchecked|1
