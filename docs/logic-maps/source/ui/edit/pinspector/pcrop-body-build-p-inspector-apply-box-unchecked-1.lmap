@format 1
@id ui-event.e916471347eceadf
@title Inspector apply box — unchecked
@section I. UI
@area UI event
@tabs Edit
@event-ref cs|PPanels/PInspectorCropUi.cs|PCropBodyBuild|pInspectorApplyBox|Unchecked|1
@entry input
@summary Handle the unchecked event exposed by PInspector.PCropBodyBuild and follow its registered code path.
@tag unchecked
@tag PInspector
@alias owner = Cadroue.UIShell.PPanels.PInspector

[input] UI event <input>
- Receive the unchecked event from the inspector apply box @ owner.PCropBodyBuild
> route

[route] Registered handler path <process>
- Update inspector apply @ owner.PInspectorApplyUpdate
- Update the visual or selection state owned by the handler @ owner.PInspectorApplyUpdate
> output

[output] UI state after the event <output>
= The unchecked event has completed through the current implementation.
! Code binding: cs|PPanels/PInspectorCropUi.cs|PCropBodyBuild|pInspectorApplyBox|Unchecked|1
