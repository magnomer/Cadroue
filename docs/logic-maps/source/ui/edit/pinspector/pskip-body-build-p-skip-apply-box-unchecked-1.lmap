@format 1
@id ui-event.8e8abae2a749e8d1
@title Skip apply box — unchecked
@section I. UI
@area UI event
@tabs Edit
@event-ref cs|PPanels/PInspectorSkip.cs|PSkipBodyBuild|pSkipApplyBox|Unchecked|1
@entry input
@summary Handle the unchecked event exposed by PInspector.PSkipBodyBuild and follow its registered code path.
@tag unchecked
@tag PInspector
@alias owner = Cadroue.UIShell.PPanels.PInspector

[input] UI event <input>
- Receive the unchecked event from the skip apply box @ owner.PSkipBodyBuild
> route

[route] Registered handler path <process>
- Update the visual or selection state owned by the handler @ owner.PSkipBodyBuild
> output

[output] UI state after the event <output>
= The unchecked event has completed through the current implementation.
! Code binding: cs|PPanels/PInspectorSkip.cs|PSkipBodyBuild|pSkipApplyBox|Unchecked|1
