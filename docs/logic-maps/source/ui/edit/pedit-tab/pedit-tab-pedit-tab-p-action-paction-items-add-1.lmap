@format 1
@id ui-event.15936ad579d439b5
@title Action — items add
@section I. UI
@area UI event
@tabs Edit
@event-ref cs|PMainArea/PEditTab.cs|PEditTab|pAction|PActionItemsAdd|1
@entry input
@summary Handle the items add event emitted by Action through the exact route registered in PEditTab.PEditTab.
@tag items add
@tag PEditTab
@alias owner = Cadroue.UIShell.PMainArea.PEditTab
@alias list = Cadroue.UIShell.PPanels.PList

[input] UI event <input>
- Receive the items add event from Action through its current registration @ owner.PEditTab
> route

[route] Registered handler path <process>
- Intersect the requested paths with unlocked list items and preserve each relay batch @ list.PListUnlockedItemsRead
- Submit those sources through the registered delegate @ owner.PEditTab
! Registered call path: LEdit.LEditAllDescribe → pList.PListUnlockedItemsRead → Where → pEditPaths.Contains → Select → LWorkSource → ToArray
> output

[output] UI state after the event <output>
= The items add event has completed through the current implementation.
! Code binding: cs|PMainArea/PEditTab.cs|PEditTab|pAction|PActionItemsAdd|1
