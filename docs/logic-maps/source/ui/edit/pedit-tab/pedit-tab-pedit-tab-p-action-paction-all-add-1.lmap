@format 1
@id ui-event.14cde8acbed91a88
@title Action — all add
@section I. UI
@area UI event
@tabs Edit
@event-ref cs|PMainArea/PEditTab.cs|PEditTab|pAction|PActionAllAdd|1
@entry input
@summary Handle the all add event emitted by Action through the exact route registered in PEditTab.PEditTab.
@tag all add
@tag PEditTab
@alias owner = Cadroue.UIShell.PMainArea.PEditTab
@alias list = Cadroue.UIShell.PPanels.PList

[input] UI event <input>
- Receive the all add event from Action through its current registration @ owner.PEditTab
> route

[route] Registered handler path <process>
- Select only unlocked list items and preserve each relay batch in the work-source request @ list.PListUnlockedItemsRead
- Submit those sources through the registered delegate @ owner.PEditTab
! Registered call path: LEdit.LEditAllDescribe → pList.PListUnlockedItemsRead → Select → LWorkSource → ToArray
> output

[output] UI state after the event <output>
= The all add event has completed through the current implementation.
! Code binding: cs|PMainArea/PEditTab.cs|PEditTab|pAction|PActionAllAdd|1
