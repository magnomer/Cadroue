@format 1
@id ui-event.2a100f32ec291ab5
@title Inspector — rotate change
@section I. UI
@area UI event
@tabs Edit
@event-ref cs|PMainArea/PEditTab.cs|PEditCropRestore|pInspector|PInspectorRotateChange|1
@entry input
@summary Handle the rotate change event emitted by Inspector through the exact route registered in PEditTab.PEditCropRestore.
@tag rotate change
@tag PEditTab
@alias owner = Cadroue.UIShell.PMainArea.PEditTab

[input] UI event <input>
- Receive the rotate change event from Inspector through its current registration @ owner.PEditCropRestore
> route

[route] Registered handler path <process>
- Restore the registered rotation handler after plan loading @ owner.PEditCropRestore
- Apply later rotation changes and refresh inspector source dimensions for the rotated orientation @ owner.PEditRotateHandle
! Registered call path: PEditRotateHandle
> output

[output] UI state after the event <output>
= The rotate change event has completed through the current implementation.
! Code binding: cs|PMainArea/PEditTab.cs|PEditCropRestore|pInspector|PInspectorRotateChange|1
