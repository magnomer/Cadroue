@format 1
@id ui-event.85db647b6c264421
@title Inspector — rotate change
@section I. UI
@area UI event
@tabs Edit
@event-ref cs|PMainArea/PEditTab.cs|PEditTab|pInspector|PInspectorRotateChange|1
@entry input
@summary Handle the rotate change event emitted by Inspector through the exact route registered in PEditTab.PEditTab.
@tag rotate change
@tag PEditTab
@alias owner = Cadroue.UIShell.PMainArea.PEditTab

[input] UI event <input>
- Receive the rotate change event from Inspector through its current registration @ owner.PEditTab
> route

[route] Registered handler path <process>
- Invoke the registered rotation handler @ owner.PEditRotateHandle
- Apply the transform to the viewer and refresh inspector source dimensions for the rotated orientation @ owner.PEditRotateHandle
! Registered call path: PEditRotateHandle
> output

[output] UI state after the event <output>
= The rotate change event has completed through the current implementation.
! Code binding: cs|PMainArea/PEditTab.cs|PEditTab|pInspector|PInspectorRotateChange|1
