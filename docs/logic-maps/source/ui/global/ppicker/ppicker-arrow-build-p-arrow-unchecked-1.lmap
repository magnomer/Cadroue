@format 1
@id ui-event.5aa609e5c7cca57a
@title Arrow — unchecked
@section I. UI
@area UI event
@tabs Global interface
@event-ref cs|PMainWindow/PPicker.cs|PPickerArrowBuild|pArrow|Unchecked|1
@entry input
@summary Handle the unchecked event exposed by PPicker.PPickerArrowBuild and follow its registered code path.
@tag unchecked
@tag PPicker
@alias owner = Cadroue.UIShell.PMainWindow.PPicker

[input] UI event <input>
- Receive the unchecked event from the arrow @ owner.PPickerArrowBuild
> route

[route] Registered handler path <process>
- Set picker open @ owner.PPickerOpenSet
> output

[output] UI state after the event <output>
= The unchecked event has completed through the current implementation.
! Code binding: cs|PMainWindow/PPicker.cs|PPickerArrowBuild|pArrow|Unchecked|1
