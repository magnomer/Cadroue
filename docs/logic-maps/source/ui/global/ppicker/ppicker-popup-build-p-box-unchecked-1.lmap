@format 1
@id ui-event.d8aec921cc122930
@title Box — unchecked
@section I. UI
@area UI event
@tabs Global interface
@event-ref cs|PMainWindow/PPicker.cs|PPickerPopupBuild|pBox|Unchecked|1
@entry input
@summary Handle the unchecked event exposed by PPicker.PPickerPopupBuild and follow its registered code path.
@tag unchecked
@tag PPicker
@alias owner = Cadroue.UIShell.PMainWindow.PPicker

[input] UI event <input>
- Receive the unchecked event from the box @ owner.PPickerPopupBuild
> route

[route] Registered handler path <process>
- Update picker summary @ owner.PPickerSummaryUpdate
- Read picker selection @ owner.PPickerSelectionRead
- Update the visual or selection state owned by the handler @ owner.PPickerSummaryUpdate
> output

[output] UI state after the event <output>
= The unchecked event has completed through the current implementation.
! Code binding: cs|PMainWindow/PPicker.cs|PPickerPopupBuild|pBox|Unchecked|1
