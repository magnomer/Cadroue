@format 1
@id ui-event.464cfab51ed2e730
@title Handle chrome maximize — click
@section I. UI
@area UI event
@tabs Global interface
@event-ref xaml|PControlBar/PToolbar.xaml|Click|PChromeMaximizeHandle|1
@entry input
@summary Handle the click event exposed by PToolbar.PChromeMaximizeHandle and follow its registered code path.
@tag click
@tag PToolbar
@alias owner = Cadroue.UIShell.PControlBar.PToolbar

[input] UI event <input>
- Dispatch the XAML click event to its named handler @ owner.PChromeMaximizeHandle
> route

[route] Registered handler path <process>
- Toggle chrome maximize @ owner.PChromeMaximizeToggle
> output

[output] UI state after the event <output>
= The click event has completed through the current implementation.
! Code binding: xaml|PControlBar/PToolbar.xaml|Click|PChromeMaximizeHandle|1
