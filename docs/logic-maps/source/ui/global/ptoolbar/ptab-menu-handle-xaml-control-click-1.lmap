@format 1
@id ui-event.d75ccb783c5197a7
@title Handle tab menu — click
@section I. UI
@area UI event
@tabs Global interface
@event-ref xaml|PControlBar/PToolbar.xaml|Click|PTabMenuHandle|1
@entry input
@summary Handle the click event exposed by PToolbar.PTabMenuHandle and follow its registered code path.
@tag click
@tag PToolbar
@alias owner = Cadroue.UIShell.PControlBar.PToolbar

[input] UI event <input>
- Dispatch the XAML click event to its named handler @ owner.PTabMenuHandle
> route

[route] Registered handler path <process>
- Execute tab menu append @ owner.PTabMenuAppend
- Evaluate the handler's implemented conditional branches @ owner.PTabMenuHandle
- Mark the routed event handled on the implemented paths @ owner.PTabMenuHandle
> output

[output] UI state after the event <output>
= The click event has completed through the current implementation.
! Code binding: xaml|PControlBar/PToolbar.xaml|Click|PTabMenuHandle|1
