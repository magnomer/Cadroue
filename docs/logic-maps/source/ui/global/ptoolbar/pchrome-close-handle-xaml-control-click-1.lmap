@format 1
@id ui-event.ecf6abe7f4c533c3
@title Handle chrome close — click
@section I. UI
@area UI event
@tabs Global interface
@event-ref xaml|PControlBar/PToolbar.xaml|Click|PChromeCloseHandle|1
@entry input
@summary Handle the click event exposed by PToolbar.PChromeCloseHandle and follow its registered code path.
@tag click
@tag PToolbar
@alias owner = Cadroue.UIShell.PControlBar.PToolbar

[input] UI event <input>
- Dispatch the XAML click event to its named handler @ owner.PChromeCloseHandle
> route

[route] Registered handler path <process>
- Continue through the implementation owned by the event handler @ owner.PChromeCloseHandle
> output

[output] UI state after the event <output>
= The click event has completed through the current implementation.
! Code binding: xaml|PControlBar/PToolbar.xaml|Click|PChromeCloseHandle|1
