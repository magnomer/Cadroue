@format 1
@id ui-event.bab66489bae1b2e0
@title Handle vertical tab load - loaded
@section I. UI
@area UI event
@tabs Global interface
@event-ref xaml|PControlBar/PTabNavigator.xaml|Loaded|PTabLoadHandle|2
@entry input
@summary Handle the loaded event exposed by the vertical PTabNavigator tab template and follow its registered code path.
@tag loaded
@tag PTabNavigator
@alias owner = Cadroue.UIShell.PControlBar.PTabNavigator

[input] UI event <input>
- Dispatch the XAML loaded event to its named handler @ owner.PTabLoadHandle
> route

[route] Registered handler path <process>
- Check tab inside @ owner.PTabInsideCheck
- Commit tab name @ owner.PTabNameCommit
- Evaluate the handler's implemented conditional branches @ owner.PTabLoadHandle
> output

[output] UI state after the event <output>
= The loaded event has completed through the current implementation.
! Code binding: xaml|PControlBar/PTabNavigator.xaml|Loaded|PTabLoadHandle|2
