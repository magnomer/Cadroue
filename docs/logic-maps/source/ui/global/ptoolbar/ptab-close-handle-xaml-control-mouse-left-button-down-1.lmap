@format 1
@id ui-event.565f04022237940a
@title Handle tab close — mouse left button down
@section I. UI
@area UI event
@tabs Global interface
@event-ref xaml|PControlBar/PToolbar.xaml|MouseLeftButtonDown|PTabCloseHandle|1
@entry input
@summary Handle the mouse left button down event exposed by PToolbar.PTabCloseHandle and follow its registered code path.
@tag mouse left button down
@tag PToolbar
@alias owner = Cadroue.UIShell.PControlBar.PToolbar

[input] UI event <input>
- Dispatch the XAML mouse left button down event to its named handler @ owner.PTabCloseHandle
> route

[route] Registered handler path <process>
- Clear tab drag @ owner.PTabDragClear
- Evaluate the handler's implemented conditional branches @ owner.PTabCloseHandle
- Mark the routed event handled on the implemented paths @ owner.PTabCloseHandle
> output

[output] UI state after the event <output>
= The mouse left button down event has completed through the current implementation.
! Code binding: xaml|PControlBar/PToolbar.xaml|MouseLeftButtonDown|PTabCloseHandle|1
