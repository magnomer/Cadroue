@format 1
@id ui-event.3bc7e9d3556c8a32
@title Handle vertical tab release - mouse left button up
@section I. UI
@area UI event
@tabs Global interface
@event-ref xaml|PControlBar/PTabNavigator.xaml|MouseLeftButtonUp|PTabReleaseHandle|2
@entry input
@summary Handle the mouse left button up event exposed by the vertical PTabNavigator tab template and follow its registered code path.
@tag mouse left button up
@tag PTabNavigator
@alias owner = Cadroue.UIShell.PControlBar.PTabNavigator

[input] UI event <input>
- Dispatch the XAML mouse left button up event to its named handler @ owner.PTabReleaseHandle
> route

[route] Registered handler path <process>
- Clear tab drag @ owner.PTabDragClear
- Check tab relay @ owner.PTabRelayCheck
- Evaluate the handler's implemented conditional branches @ owner.PTabReleaseHandle
- Mark the routed event handled on the implemented paths @ owner.PTabReleaseHandle
> output

[output] UI state after the event <output>
= The mouse left button up event has completed through the current implementation.
! Code binding: xaml|PControlBar/PTabNavigator.xaml|MouseLeftButtonUp|PTabReleaseHandle|2
