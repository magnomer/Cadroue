@format 1
@id ui-event.211acfebcfb912a3
@title Tab add menu item — click
@section I. UI
@area UI event
@tabs Global interface
@event-ref cs|PControlBar/PToolbarTab.cs|PTabMenuAppend|pTabAddMenuItem|Click|1
@entry input
@summary Handle the click event exposed by PTabNavigator.PTabMenuAppend and follow its registered code path.
@tag click
@tag PTabNavigator
@alias owner = Cadroue.UIShell.PControlBar.PTabNavigator

[input] UI event <input>
- Receive the click event from the tab add menu item @ owner.PTabMenuAppend
> route

[route] Registered handler path <process>
- Add tab layout @ owner.PTabLayoutAdd
- Evaluate the handler's implemented conditional branches @ owner.PTabLayoutAdd
> output

[output] UI state after the event <output>
= The click event has completed through the current implementation.
! Code binding: cs|PControlBar/PToolbarTab.cs|PTabMenuAppend|pTabAddMenuItem|Click|1
