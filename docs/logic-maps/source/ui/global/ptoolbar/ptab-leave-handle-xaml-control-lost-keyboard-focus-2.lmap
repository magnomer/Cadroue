@format 1
@id ui-event.deac241b8f23773f
@title Handle vertical tab leave - lost keyboard focus
@section I. UI
@area UI event
@tabs Global interface
@event-ref xaml|PControlBar/PTabNavigator.xaml|LostKeyboardFocus|PTabLeaveHandle|2
@entry input
@summary Handle the lost keyboard focus event exposed by the vertical PTabNavigator tab template and follow its registered code path.
@tag lost keyboard focus
@tag PTabNavigator
@alias owner = Cadroue.UIShell.PControlBar.PTabNavigator

[input] UI event <input>
- Dispatch the XAML lost keyboard focus event to its named handler @ owner.PTabLeaveHandle
> route

[route] Registered handler path <process>
- Commit tab name @ owner.PTabNameCommit
- Evaluate the handler's implemented conditional branches @ owner.PTabLeaveHandle
> output

[output] UI state after the event <output>
= The lost keyboard focus event has completed through the current implementation.
! Code binding: xaml|PControlBar/PTabNavigator.xaml|LostKeyboardFocus|PTabLeaveHandle|2
