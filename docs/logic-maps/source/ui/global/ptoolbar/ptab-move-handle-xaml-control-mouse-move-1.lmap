@format 1
@id ui-event.34ce4036b7dc3f6c
@title Handle tab move — mouse move
@section I. UI
@area UI event
@tabs Global interface
@event-ref xaml|PControlBar/PTabNavigator.xaml|MouseMove|PTabMoveHandle|1
@entry input
@summary Handle the mouse move event exposed by PTabNavigator.PTabMoveHandle and follow its registered code path.
@tag mouse move
@tag PTabNavigator
@alias owner = Cadroue.UIShell.PControlBar.PTabNavigator
@alias tabset = Cadroue.UIShell.PControlBar.LTabset

[input] UI event <input>
- Dispatch the XAML mouse move event to its named handler @ owner.PTabMoveHandle
> route

[route] Registered handler path <process>
- Show and synchronize the shared drag ghost after the pointer crosses the drag threshold @ owner.PTabMoveHandle
- Resolve tab index @ owner.PTabIndexResolve
- Reorder the real tab collection for the resolved index @ tabset.LTabsetMove
- Evaluate the handler's implemented conditional branches @ owner.PTabMoveHandle
- Mark the routed event handled on the implemented paths @ owner.PTabMoveHandle
> output

[output] UI state after the event <output>
= The mouse move event has completed through the current implementation.
! Code binding: xaml|PControlBar/PTabNavigator.xaml|MouseMove|PTabMoveHandle|1
