@format 1
@id ui-event.4565161255996a07
@title Handle vertical tab key - key down
@section I. UI
@area UI event
@tabs Global interface
@event-ref xaml|PControlBar/PTabNavigator.xaml|KeyDown|PTabKeyHandle|2
@entry input
@summary Handle the key down event exposed by the vertical PTabNavigator tab template and follow its registered code path.
@tag key down
@tag PTabNavigator
@alias owner = Cadroue.UIShell.PControlBar.PTabNavigator

[input] UI event <input>
- Dispatch the XAML key down event to its named handler @ owner.PTabKeyHandle
> route

[route] Registered handler path <process>
- Commit tab name @ owner.PTabNameCommit
- Evaluate the handler's implemented conditional branches @ owner.PTabKeyHandle
- Mark the routed event handled on the implemented paths @ owner.PTabKeyHandle
> output

[output] UI state after the event <output>
= The key down event has completed through the current implementation.
! Code binding: xaml|PControlBar/PTabNavigator.xaml|KeyDown|PTabKeyHandle|2
