@format 1
@id ui-event.e3e16f8fbe8456ea
@title Handle chrome minimize — click
@section I. UI
@area UI event
@tabs Global interface
@event-ref xaml|PControlBar/PToolbar.xaml|Click|PChromeMinimizeHandle|1
@entry input
@summary Handle the click event exposed by PToolbar.PChromeMinimizeHandle and follow its registered code path.
@tag click
@tag PToolbar
@alias owner = Cadroue.UIShell.PControlBar.PToolbar

[input] UI event <input>
- Dispatch the XAML click event to its named handler @ owner.PChromeMinimizeHandle
> route

[route] Registered handler path <process>
- Continue through the implementation owned by the event handler @ owner.PChromeMinimizeHandle
> output

[output] UI state after the event <output>
= The click event has completed through the current implementation.
! Code binding: xaml|PControlBar/PToolbar.xaml|Click|PChromeMinimizeHandle|1
