@format 1
@id ui-event.deac241b8f23773e
@title Handle tab leave — lost keyboard focus
@section I. UI
@area UI event
@tabs Global interface
@event-ref xaml|PControlBar/PToolbar.xaml|LostKeyboardFocus|PTabLeaveHandle|1
@entry input
@summary Handle the lost keyboard focus event exposed by PToolbar.PTabLeaveHandle and follow its registered code path.
@tag lost keyboard focus
@tag PToolbar
@alias owner = Cadroue.UIShell.PControlBar.PToolbar

[input] UI event <input>
- Dispatch the XAML lost keyboard focus event to its named handler @ owner.PTabLeaveHandle
> route

[route] Registered handler path <process>
- Commit tab name @ owner.PTabNameCommit
- Evaluate the handler's implemented conditional branches @ owner.PTabLeaveHandle
> output

[output] UI state after the event <output>
= The lost keyboard focus event has completed through the current implementation.
! Code binding: xaml|PControlBar/PToolbar.xaml|LostKeyboardFocus|PTabLeaveHandle|1
