@format 1
@id ui-event.9c5da99e0cc0ce47
@title Console — unloaded
@section I. UI
@area UI event
@tabs Global interface
@event-ref cs|PMainArea/PConsole.cs|PConsole|this|Unloaded|1
@entry input
@summary Handle the unloaded event emitted by Console through the exact route registered in PConsole.PConsole.
@tag unloaded
@tag PConsole
@alias owner = Cadroue.UIShell.PMainArea.PConsole

[input] UI event <input>
- Receive the unloaded event from Console through its current registration @ owner.PConsole
> route

[route] Registered handler path <process>
- Invoke the registered owner handler @ owner.PConsoleUnloadHandle
! Registered call path: PConsoleUnloadHandle
> output

[output] UI state after the event <output>
= The unloaded event has completed through the current implementation.
! Code binding: cs|PMainArea/PConsole.cs|PConsole|this|Unloaded|1
