@format 1
@id ui-event.c8b2894bc6948181
@title Timer — tick
@section I. UI
@area UI event
@tabs Global interface
@event-ref cs|PMainArea/PConsoleScene.cs|PConsoleTimerCreate|pTimer|Tick|1
@entry input
@summary Handle the tick event emitted by Timer through the exact route registered in PConsole.PConsoleTimerCreate.
@tag tick
@tag PConsole
@alias owner = Cadroue.UIShell.PMainArea.PConsole

[input] UI event <input>
- Receive the tick event from Timer through its current registration @ owner.PConsoleTimerCreate
> route

[route] Registered handler path <process>
- Invoke the registered owner handler @ owner.PConsoleTickHandle
! Registered call path: PConsoleTickHandle
> output

[output] UI state after the event <output>
= The tick event has completed through the current implementation.
! Code binding: cs|PMainArea/PConsoleScene.cs|PConsoleTimerCreate|pTimer|Tick|1
