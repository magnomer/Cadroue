@format 1
@id ui-event.3c0c34df4fef5aef
@title Station — change
@section I. UI
@area UI event
@tabs Global interface
@event-ref cs|PMainArea/PConsole.cs|PConsole|LStation|LStationChange|1
@entry input
@summary Handle the change event emitted by Station through the exact route registered in PConsole.PConsole.
@tag change
@tag PConsole
@alias owner = Cadroue.UIShell.PMainArea.PConsole

[input] UI event <input>
- Receive the change event from Station through its current registration @ owner.PConsole
> route

[route] Registered handler path <process>
- Invoke the registered owner handler @ owner.PConsoleStationHandle
! Registered call path: PConsoleStationHandle
> output

[output] UI state after the event <output>
= The change event has completed through the current implementation.
! Code binding: cs|PMainArea/PConsole.cs|PConsole|LStation|LStationChange|1
