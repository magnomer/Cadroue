@format 1
@id ui-event.e529dc9caf79c29d
@title Button — click
@section I. UI
@area UI event
@tabs Global interface
@event-ref cs|PMainArea/PConsole.cs|PConsoleButtonBuild|pButton|Click|1
@entry input
@summary Handle the click event exposed by PConsole.PConsoleButtonBuild and follow its registered code path.
@tag click
@tag PConsole
@alias owner = Cadroue.UIShell.PMainArea.PConsole

[input] UI event <input>
- Receive the click event from the button @ owner.PConsoleButtonBuild
> route

[route] Registered handler path <process>
- Update the visual or selection state owned by the handler @ owner.PConsoleButtonBuild
> output

[output] UI state after the event <output>
= The click event has completed through the current implementation.
! Code binding: cs|PMainArea/PConsole.cs|PConsoleButtonBuild|pButton|Click|1
