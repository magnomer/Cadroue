@format 1
@id ui-event.e97296749ad8e1ff
@title Button — click
@section I. UI
@area UI event
@tabs Global interface
@event-ref cs|PMainArea/PConsole.cs|PConsoleSwitchBuild|pButton|Click|1
@entry input
@summary Handle the click event exposed by PConsole.PConsoleSwitchBuild and follow its registered code path.
@tag click
@tag PConsole
@alias owner = Cadroue.UIShell.PMainArea.PConsole

[input] UI event <input>
- Receive the click event from the button @ owner.PConsoleSwitchBuild
> route

[route] Registered handler path <process>
- Continue through the implementation owned by the event handler @ owner.PConsoleSwitchBuild
> output

[output] UI state after the event <output>
= The click event has completed through the current implementation.
! Code binding: cs|PMainArea/PConsole.cs|PConsoleSwitchBuild|pButton|Click|1
