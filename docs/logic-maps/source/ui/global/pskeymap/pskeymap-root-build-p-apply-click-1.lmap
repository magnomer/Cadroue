@format 1
@id ui-event.d704c04c4560e8af
@title Apply — click
@section I. UI
@area UI event
@tabs Global interface
@event-ref cs|PSKeymap/PSKeymap.cs|PSKeymapRootBuild|pApply|Click|1
@entry input
@summary Handle the click event exposed by PSKeymap.PSKeymapRootBuild and follow its registered code path.
@tag click
@tag PSKeymap
@alias owner = Cadroue.UIShell.PSKeymap

[input] UI event <input>
- Receive the click event from the apply @ owner.PSKeymapRootBuild
> route

[route] Registered handler path <process>
- Apply ps keymap @ owner.PSKeymapApply
> output

[output] UI state after the event <output>
= The click event has completed through the current implementation.
! Code binding: cs|PSKeymap/PSKeymap.cs|PSKeymapRootBuild|pApply|Click|1
