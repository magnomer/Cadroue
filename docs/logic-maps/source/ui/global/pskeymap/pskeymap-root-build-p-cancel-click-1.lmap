@format 1
@id ui-event.8af9e8b883d9c324
@title Cancel — click
@section I. UI
@area UI event
@tabs Global interface
@event-ref cs|PSKeymap/PSKeymap.cs|PSKeymapRootBuild|pCancel|Click|1
@entry input
@summary Handle the click event exposed by PSKeymap.PSKeymapRootBuild and follow its registered code path.
@tag click
@tag PSKeymap
@alias owner = Cadroue.UIShell.PSKeymap

[input] UI event <input>
- Receive the click event from the cancel @ owner.PSKeymapRootBuild
> route

[route] Registered handler path <process>
- Update the visual or selection state owned by the handler @ owner.PSKeymapRootBuild
> output

[output] UI state after the event <output>
= The click event has completed through the current implementation.
! Code binding: cs|PSKeymap/PSKeymap.cs|PSKeymapRootBuild|pCancel|Click|1
