@format 1
@id ui-event.bdf1cdbd9e51219d
@title Reset — click
@section I. UI
@area UI event
@tabs Global interface
@event-ref cs|PSKeymap/PSKeymap.cs|PSKeymapRootBuild|pReset|Click|1
@entry input
@summary Handle the click event exposed by PSKeymap.PSKeymapRootBuild and follow its registered code path.
@tag click
@tag PSKeymap
@alias owner = Cadroue.UIShell.PSKeymap

[input] UI event <input>
- Receive the click event from the reset @ owner.PSKeymapRootBuild
> route

[route] Registered handler path <process>
- Apply ps keymap default @ owner.PSKeymapDefaultApply
> output

[output] UI state after the event <output>
= The click event has completed through the current implementation.
! Code binding: cs|PSKeymap/PSKeymap.cs|PSKeymapRootBuild|pReset|Click|1
