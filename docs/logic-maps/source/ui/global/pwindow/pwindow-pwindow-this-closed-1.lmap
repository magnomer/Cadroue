@format 1
@id ui-event.b30cdf6d83460640
@title Window — closed
@section I. UI
@area UI event
@tabs Global interface
@event-ref cs|PMainWindow/PWindow.xaml.cs|PWindow|this|Closed|1
@entry input
@summary Handle the closed event emitted by Window through the exact route registered in PWindow.PWindow.
@tag closed
@tag PWindow
@alias owner = Cadroue.UIShell.PMainWindow.PWindow

[input] UI event <input>
- Receive the closed event from Window through its current registration @ owner.PWindow
> route

[route] Registered handler path <process>
- Invoke the registered owner handler @ owner.PWindowCloseHandle
! Registered call path: PWindowCloseHandle
> output

[output] UI state after the event <output>
= The closed event has completed through the current implementation.
! Code binding: cs|PMainWindow/PWindow.xaml.cs|PWindow|this|Closed|1
