@format 1
@id ui-event.2b73fc568e844071
@title Button — click
@section I. UI
@area UI event
@tabs Global interface
@event-ref cs|PSOptions/PSOptionsTimeline.cs|PSSpectrumButtonBuild|pButton|Click|1
@entry input
@summary Handle the click event exposed by PSOptions.PSSpectrumButtonBuild and follow its registered code path.
@tag click
@tag PSOptions
@alias owner = Cadroue.UIShell.PSOptions

[input] UI event <input>
- Receive the click event from the button @ owner.PSSpectrumButtonBuild
> route

[route] Registered handler path <process>
- Continue through the implementation owned by the event handler @ owner.PSSpectrumButtonBuild
> output

[output] UI state after the event <output>
= The click event has completed through the current implementation.
! Code binding: cs|PSOptions/PSOptionsTimeline.cs|PSSpectrumButtonBuild|pButton|Click|1
