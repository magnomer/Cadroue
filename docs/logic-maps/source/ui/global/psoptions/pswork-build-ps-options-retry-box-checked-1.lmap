@format 1
@id ui-event.7d279e52c7c2145a
@title Options retry box — checked
@section I. UI
@area UI event
@tabs Global interface
@event-ref cs|PSOptions/PSOptions.cs|PSWorkBuild|psOptionsRetryBox|Checked|1
@entry input
@summary Handle the checked event exposed by PSOptions.PSWorkBuild and follow its registered code path.
@tag checked
@tag PSOptions
@alias owner = Cadroue.UIShell.PSOptions

[input] UI event <input>
- Receive the checked event from the options retry box @ owner.PSWorkBuild
> route

[route] Registered handler path <process>
- Update the visual or selection state owned by the handler @ owner.PSWorkBuild
> output

[output] UI state after the event <output>
= The checked event has completed through the current implementation.
! Code binding: cs|PSOptions/PSOptions.cs|PSWorkBuild|psOptionsRetryBox|Checked|1
