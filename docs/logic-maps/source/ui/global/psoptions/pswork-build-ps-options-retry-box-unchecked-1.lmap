@format 1
@id ui-event.2a0c2ae531362084
@title Options retry box — unchecked
@section I. UI
@area UI event
@tabs Global interface
@event-ref cs|PSOptions/PSOptions.cs|PSWorkBuild|psOptionsRetryBox|Unchecked|1
@entry input
@summary Handle the unchecked event exposed by PSOptions.PSWorkBuild and follow its registered code path.
@tag unchecked
@tag PSOptions
@alias owner = Cadroue.UIShell.PSOptions

[input] UI event <input>
- Receive the unchecked event from the options retry box @ owner.PSWorkBuild
> route

[route] Registered handler path <process>
- Update the visual or selection state owned by the handler @ owner.PSWorkBuild
> output

[output] UI state after the event <output>
= The unchecked event has completed through the current implementation.
! Code binding: cs|PSOptions/PSOptions.cs|PSWorkBuild|psOptionsRetryBox|Unchecked|1
