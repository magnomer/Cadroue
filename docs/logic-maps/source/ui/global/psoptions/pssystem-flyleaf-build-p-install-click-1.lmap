@format 1
@id ui-event.1f6f3b5965c53190
@title Install — click
@section I. UI
@area UI event
@tabs Global interface
@event-ref cs|PSOptions/PSOptionsSystem.cs|PSSystemFlyleafBuild|pInstall|Click|1
@entry input
@summary Handle the click event exposed by PSOptions.PSSystemFlyleafBuild and follow its registered code path.
@tag click
@tag PSOptions
@alias owner = Cadroue.UIShell.PSOptions

[input] UI event <input>
- Receive the click event from the install @ owner.PSSystemFlyleafBuild
> route

[route] Registered handler path <process>
- Update the visual or selection state owned by the handler @ owner.PSSystemFlyleafBuild
> output

[output] UI state after the event <output>
= The click event has completed through the current implementation.
! Code binding: cs|PSOptions/PSOptionsSystem.cs|PSSystemFlyleafBuild|pInstall|Click|1
