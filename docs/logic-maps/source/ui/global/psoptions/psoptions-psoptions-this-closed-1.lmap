@format 1
@id ui-event.8ce8e4baff66d327
@title S Options — closed
@section I. UI
@area UI event
@tabs Global interface
@event-ref cs|PSOptions/PSOptions.cs|PSOptions|this|Closed|1
@entry input
@summary Handle the closed event emitted by S Options through the exact route registered in PSOptions.PSOptions.
@tag closed
@tag PSOptions
@alias owner = Cadroue.UIShell.PSOptions

[input] UI event <input>
- Receive the closed event from S Options through its current registration @ owner.PSOptions
> route

[route] Registered handler path <process>
- Invoke the registered owner handler @ owner.PSOptionsCloseHandle
! Registered call path: PSOptionsCloseHandle
> output

[output] UI state after the event <output>
= The closed event has completed through the current implementation.
! Code binding: cs|PSOptions/PSOptions.cs|PSOptions|this|Closed|1
