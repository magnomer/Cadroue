@format 1
@id ui-event.cfdd15170c693a1f
@title Cancel — click
@section I. UI
@area UI event
@tabs Global interface
@event-ref cs|PSOptions/PSOptions.cs|PSOptionsRootBuild|pCancel|Click|1
@entry input
@summary Handle the click event exposed by PSOptions.PSOptionsRootBuild and follow its registered code path.
@tag click
@tag PSOptions
@alias owner = Cadroue.UIShell.PSOptions

[input] UI event <input>
- Receive the click event from the cancel @ owner.PSOptionsRootBuild
> route

[route] Registered handler path <process>
- Update the visual or selection state owned by the handler @ owner.PSOptionsRootBuild
> output

[output] UI state after the event <output>
= The click event has completed through the current implementation.
! Code binding: cs|PSOptions/PSOptions.cs|PSOptionsRootBuild|pCancel|Click|1
