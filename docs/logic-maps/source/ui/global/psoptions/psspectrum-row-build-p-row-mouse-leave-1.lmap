@format 1
@id ui-event.6c2c62a5d0be3d9b
@title Row — mouse leave
@section I. UI
@area UI event
@tabs Global interface
@event-ref cs|PSOptions/PSOptionsTimeline.cs|PSSpectrumRowBuild|pRow|MouseLeave|1
@entry input
@summary Handle the mouse leave event exposed by PSOptions.PSSpectrumRowBuild and follow its registered code path.
@tag mouse leave
@tag PSOptions
@alias owner = Cadroue.UIShell.PSOptions

[input] UI event <input>
- Receive the mouse leave event from the row @ owner.PSSpectrumRowBuild
> route

[route] Registered handler path <process>
- Apply ps spectrum hover @ owner.PSSpectrumHoverApply
- Evaluate the handler's implemented conditional branches @ owner.PSSpectrumHoverApply
- Update the visual or selection state owned by the handler @ owner.PSSpectrumHoverApply
> output

[output] UI state after the event <output>
= The mouse leave event has completed through the current implementation.
! Code binding: cs|PSOptions/PSOptionsTimeline.cs|PSSpectrumRowBuild|pRow|MouseLeave|1
