@format 1
@id ui-event.43d9b1c3bd690ab2
@title Apply — click
@section I. UI
@area UI event
@tabs Global interface
@event-ref cs|PSOptions/PSOptions.cs|PSOptionsRootBuild|pApply|Click|1
@entry input
@summary Handle the click event exposed by PSOptions.PSOptionsRootBuild and follow its registered code path.
@tag click
@tag PSOptions
@alias owner = Cadroue.UIShell.PSOptions

[input] UI event <input>
- Receive the click event from the apply @ owner.PSOptionsRootBuild
> route

[route] Registered handler path <process>
- Apply ps options @ owner.PSOptionsApply
- Evaluate the handler's implemented conditional branches @ owner.PSOptionsApply
> output

[output] UI state after the event <output>
= The click event has completed through the current implementation.
! Code binding: cs|PSOptions/PSOptions.cs|PSOptionsRootBuild|pApply|Click|1
