@format 1
@id ui-event.e9600cff9cb369ae
@title Log verbose — unchecked
@section I. UI
@area UI event
@tabs Global interface
@event-ref cs|PLog/PLogContent.cs|PLogVerboseBuild|pLogVerbose|Unchecked|1
@entry input
@summary Handle the unchecked event exposed by PLogWindow.PLogVerboseBuild and follow its registered code path.
@tag unchecked
@tag PLogWindow
@alias owner = Cadroue.UIShell.PLogWindow

[input] UI event <input>
- Receive the unchecked event from the log verbose @ owner.PLogVerboseBuild
> route

[route] Registered handler path <process>
- Set log verbose @ owner.PLogVerboseSet
- Apply log category @ owner.PLogCategoryApply
- Evaluate the handler's implemented conditional branches @ owner.PLogVerboseSet
> output

[output] UI state after the event <output>
= The unchecked event has completed through the current implementation.
! Code binding: cs|PLog/PLogContent.cs|PLogVerboseBuild|pLogVerbose|Unchecked|1
