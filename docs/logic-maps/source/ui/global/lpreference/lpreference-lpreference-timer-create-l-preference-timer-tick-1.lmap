@format 1
@id ui-event.9f2464b27e981eca
@title Preference Timer — tick
@section I. UI
@area UI event
@tabs Global interface
@event-ref cs|LPreference.cs|LPreferenceTimerCreate|lPreferenceTimer|Tick|1
@entry input
@summary Handle the tick event emitted by Preference Timer through the exact route registered in LPreference.LPreferenceTimerCreate.
@tag tick
@tag LPreference
@alias owner = Cadroue.UIShell.LPreference

[input] UI event <input>
- Receive the tick event from Preference Timer through its current registration @ owner.LPreferenceTimerCreate
> route

[route] Registered handler path <process>
- Invoke the registered owner handler @ owner.if
- Continue through the owner callback @ owner.foreach
! Registered call path: lPreferenceTimer.Stop → LPreferenceStateStore.LPreferenceStateSave → LPreferenceStateCurrent.LPreferenceDifferenceRead → LTraceLog.LTraceInfoRecord
> output

[output] UI state after the event <output>
= The tick event has completed through the current implementation.
! Code binding: cs|LPreference.cs|LPreferenceTimerCreate|lPreferenceTimer|Tick|1
