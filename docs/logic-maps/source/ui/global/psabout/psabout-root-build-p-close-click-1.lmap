@format 1
@id ui-event.89abc93010305c52
@title Close — click
@section I. UI
@area UI event
@tabs Global interface
@event-ref cs|PSAbout/PSAbout.cs|PSAboutRootBuild|pClose|Click|1
@entry input
@summary Handle the click event exposed by PSAbout.PSAboutRootBuild and follow its registered code path.
@tag click
@tag PSAbout
@alias owner = Cadroue.UIShell.PSAbout

[input] UI event <input>
- Receive the click event from the close @ owner.PSAboutRootBuild
> route

[route] Registered handler path <process>
- Update the visual or selection state owned by the handler @ owner.PSAboutRootBuild
> output

[output] UI state after the event <output>
= The click event has completed through the current implementation.
! Code binding: cs|PSAbout/PSAbout.cs|PSAboutRootBuild|pClose|Click|1
