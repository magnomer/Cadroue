@format 1
@id ui-event.7311f818a8ac3088
@title Button — mouse left button down
@section I. UI
@area UI event
@tabs Worklist
@event-ref cs|PMainArea/PRosterCard.cs|PRosterCloseBuild|pButton|MouseLeftButtonDown|1
@entry input
@summary Handle the mouse left button down event exposed by PRoster.PRosterCloseBuild and follow its registered code path.
@tag mouse left button down
@tag PRoster
@alias owner = Cadroue.UIShell.PMainArea.PRoster

[input] UI event <input>
- Receive the mouse left button down event from the button @ owner.PRosterCloseBuild
> route

[route] Registered handler path <process>
- Mark the routed event handled on the implemented paths @ owner.PRosterCloseBuild
- Update the visual or selection state owned by the handler @ owner.PRosterCloseBuild
> output

[output] UI state after the event <output>
= The mouse left button down event has completed through the current implementation.
! Code binding: cs|PMainArea/PRosterCard.cs|PRosterCloseBuild|pButton|MouseLeftButtonDown|1
