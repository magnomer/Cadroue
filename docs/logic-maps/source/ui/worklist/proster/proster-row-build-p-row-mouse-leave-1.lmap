@format 1
@id ui-event.ed67b2a56bd4445c
@title Row — mouse leave
@section I. UI
@area UI event
@tabs Worklist
@event-ref cs|PMainArea/PRosterQueue.cs|PRosterRowBuild|pRow|MouseLeave|1
@entry input
@summary Handle the mouse leave event exposed by PRoster.PRosterRowBuild and follow its registered code path.
@tag mouse leave
@tag PRoster
@alias owner = Cadroue.UIShell.PMainArea.PRoster

[input] UI event <input>
- Receive the mouse leave event from the row @ owner.PRosterRowBuild
> route

[route] Registered handler path <process>
- Apply roster hover @ owner.PRosterHoverApply
- Read roster shade @ owner.PRosterShadeRead
- Evaluate the handler's implemented conditional branches @ owner.PRosterHoverApply
- Update the visual or selection state owned by the handler @ owner.PRosterHoverApply
> output

[output] UI state after the event <output>
= The mouse leave event has completed through the current implementation.
! Code binding: cs|PMainArea/PRosterQueue.cs|PRosterRowBuild|pRow|MouseLeave|1
