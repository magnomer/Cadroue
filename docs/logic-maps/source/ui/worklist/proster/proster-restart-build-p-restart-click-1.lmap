@format 1
@id ui-event.2f744c3cda9c6899
@title Restart — click
@section I. UI
@area UI event
@tabs Worklist
@event-ref cs|PMainArea/PRosterMenu.cs|PRosterRestartBuild|pRestart|Click|1
@entry input
@summary Handle the click event exposed by PRoster.PRosterRestartBuild and follow its registered code path.
@tag click
@tag PRoster
@alias owner = Cadroue.UIShell.PMainArea.PRoster

[input] UI event <input>
- Receive the click event from the restart @ owner.PRosterRestartBuild
> route

[route] Registered handler path <process>
- Evaluate the handler's implemented conditional branches @ owner.PRosterRestartBuild
- Update the visual or selection state owned by the handler @ owner.PRosterRestartBuild
> output

[output] UI state after the event <output>
= The click event has completed through the current implementation.
! Code binding: cs|PMainArea/PRosterMenu.cs|PRosterRestartBuild|pRestart|Click|1
