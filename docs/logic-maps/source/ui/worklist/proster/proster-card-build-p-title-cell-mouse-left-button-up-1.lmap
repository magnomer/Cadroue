@format 1
@id ui-event.7e08c1c53cab55b3
@title Title cell — mouse left button up
@section I. UI
@area UI event
@tabs Worklist
@event-ref cs|PMainArea/PRosterCard.cs|PRosterCardBuild|pTitleCell|MouseLeftButtonUp|1
@entry input
@summary Handle the mouse left button up event exposed by PRoster.PRosterCardBuild and follow its registered code path.
@tag mouse left button up
@tag PRoster
@alias owner = Cadroue.UIShell.PMainArea.PRoster

[input] UI event <input>
- Receive the mouse left button up event from the title cell @ owner.PRosterCardBuild
> route

[route] Registered handler path <process>
- Select roster card @ owner.PRosterCardSelect
- Apply roster select @ owner.PRosterSelectApply
- Apply roster card @ owner.PRosterCardApply
- Handle roster select @ owner.PRosterSelectHandle
> output

[output] UI state after the event <output>
= The mouse left button up event has completed through the current implementation.
! Code binding: cs|PMainArea/PRosterCard.cs|PRosterCardBuild|pTitleCell|MouseLeftButtonUp|1
