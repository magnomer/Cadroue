@format 1
@id ui-event.ac138107cf279e03
@title Button — mouse leave
@section I. UI
@area UI event
@tabs Worklist
@event-ref cs|PMainArea/PRosterCard.cs|PRosterCloseBuild|pButton|MouseLeave|1
@entry input
@summary Handle the mouse leave event exposed by PRoster.PRosterCloseBuild and follow its registered code path.
@tag mouse leave
@tag PRoster
@alias owner = Cadroue.UIShell.PMainArea.PRoster

[input] UI event <input>
- Receive the mouse leave event from the button @ owner.PRosterCloseBuild
> route

[route] Registered handler path <process>
- Mark the routed event handled on the implemented paths @ owner.PRosterCloseBuild
- Update the visual or selection state owned by the handler @ owner.PRosterCloseBuild
> output

[output] UI state after the event <output>
= The mouse leave event has completed through the current implementation.
! Code binding: cs|PMainArea/PRosterCard.cs|PRosterCloseBuild|pButton|MouseLeave|1
