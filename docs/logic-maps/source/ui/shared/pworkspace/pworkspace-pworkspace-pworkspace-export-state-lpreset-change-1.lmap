@format 1
@id ui-event.4d3cdbf09cfb6c90
@title Workspace Export State — preset change
@section I. UI
@area UI event
@tabs Split, Edit, Convert, Audio, Funnel
@event-ref cs|PControlBar/PWorkspace.cs|PWorkspace|PWorkspaceExportState|LPresetChange|1
@entry input
@summary Handle the preset change event emitted by Workspace Export State through the exact route registered in PWorkspace.PWorkspace.
@tag preset change
@tag PWorkspace
@alias owner = Cadroue.UIShell.PControlBar.PWorkspace

[input] UI event <input>
- Receive the preset change event from Workspace Export State through its current registration @ owner.PWorkspace
> route

[route] Registered handler path <process>
- Invoke the registered owner handler @ owner.PWorkspaceExportHandle
! Registered call path: PWorkspaceExportHandle
> output

[output] UI state after the event <output>
= The preset change event has completed through the current implementation.
! Code binding: cs|PControlBar/PWorkspace.cs|PWorkspace|PWorkspaceExportState|LPresetChange|1
