@format 1
@id ui-event.1dc0ca0d2b58f6cd
@title Toggle button — click
@section I. UI
@area UI event
@tabs Split, Edit, Convert, Audio, Funnel
@event-ref cs|PControlBar/PWorkspace.cs|PWorkspaceInfoBuild|pToggleButton|Click|1
@entry input
@summary Handle the click event exposed by PWorkspace.PWorkspaceInfoBuild and follow its registered code path.
@tag click
@tag PWorkspace
@alias owner = Cadroue.UIShell.PControlBar.PWorkspace

[input] UI event <input>
- Receive the click event from the toggle button @ owner.PWorkspaceInfoBuild
> route

[route] Registered handler path <process>
- Update the visual or selection state owned by the handler @ owner.PWorkspaceInfoBuild
> output

[output] UI state after the event <output>
= The click event has completed through the current implementation.
! Code binding: cs|PControlBar/PWorkspace.cs|PWorkspaceInfoBuild|pToggleButton|Click|1
