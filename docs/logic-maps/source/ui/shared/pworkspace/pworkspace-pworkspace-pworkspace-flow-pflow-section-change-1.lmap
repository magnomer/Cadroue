@format 1
@id ui-event.3b74fe919b5b52fa
@title Workspace Flow — flow section change
@section I. UI
@area UI event
@tabs Split, Edit, Convert, Audio, Funnel
@event-ref cs|PControlBar/PWorkspace.cs|PWorkspace|PWorkspaceFlow|PFlowSectionChange|1
@entry input
@summary Handle the flow section change event emitted by Workspace Flow through the exact route registered in PWorkspace.PWorkspace.
@tag flow section change
@tag PWorkspace
@alias owner = Cadroue.UIShell.PControlBar.PWorkspace

[input] UI event <input>
- Receive the flow section change event from Workspace Flow through its current registration @ owner.PWorkspace
> route

[route] Registered handler path <process>
- Invoke the registered owner handler @ owner.PWorkspaceSectionHandle
! Registered call path: PWorkspaceSectionHandle
> output

[output] UI state after the event <output>
= The flow section change event has completed through the current implementation.
! Code binding: cs|PControlBar/PWorkspace.cs|PWorkspace|PWorkspaceFlow|PFlowSectionChange|1
