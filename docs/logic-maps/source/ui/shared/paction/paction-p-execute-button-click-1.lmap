@format 1
@id ui-event.d251ab3132593e98
@title Execute button — click
@section I. UI
@area UI event
@tabs Split, Edit, Convert, Audio, Merge, Funnel
@event-ref cs|PMainArea/PAction.cs|PAction|pExecuteButton|Click|1
@entry input
@summary Handle the click event exposed by PAction.PAction and follow its registered code path.
@tag click
@tag PAction
@alias owner = Cadroue.UIShell.PMainArea.PAction

[input] UI event <input>
- Receive the click event from the execute button @ owner.PAction
> route

[route] Registered handler path <process>
- Update the visual or selection state owned by the handler @ owner.PAction
> output

[output] UI state after the event <output>
= The click event has completed through the current implementation.
! Code binding: cs|PMainArea/PAction.cs|PAction|pExecuteButton|Click|1
