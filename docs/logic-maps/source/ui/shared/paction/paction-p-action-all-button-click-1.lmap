@format 1
@id ui-event.9d08a92f070b2b8e
@title Action all button — click
@section I. UI
@area UI event
@tabs Split, Edit, Convert, Audio, Merge, Funnel
@event-ref cs|PMainArea/PAction.cs|PAction|pActionAllButton|Click|1
@entry input
@summary Handle the click event exposed by PAction.PAction and follow its registered code path.
@tag click
@tag PAction
@alias owner = Cadroue.UIShell.PMainArea.PAction
@alias courier = Cadroue.UIShell.PMainArea.LCourier

[input] UI event <input>
- Receive the click event from the action all button @ owner.PAction
> route

[route] Registered handler path <process>
- Build the tab-specific work descriptions through the registered handler @ owner.PAction
- At the scheduling boundary, snapshot the complete relay graph and stage settings for each submitted cohort @ courier.LCourierScheduleAdd(...)
> output

[output] UI state after the event <output>
= Immediate work is queued with stable relay-stage identifiers; later tab changes cannot rewrite its saved route.
! Code binding: cs|PMainArea/PAction.cs|PAction|pActionAllButton|Click|1
