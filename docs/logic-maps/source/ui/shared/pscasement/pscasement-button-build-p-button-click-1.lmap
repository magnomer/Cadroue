@format 1
@id ui-event.f5d57ad9165ced24
@title Button — click
@section I. UI
@area UI event
@tabs Split, Edit, Convert, Audio, Merge, Global interface
@event-ref cs|PSShared/PSCasement.cs|PSCasementButtonBuild|pButton|Click|1
@entry input
@summary Handle the click event exposed by PSCasement.PSCasementButtonBuild and follow its registered code path.
@tag click
@tag PSCasement
@alias owner = Cadroue.UIShell.PSShared.PSCasement

[input] UI event <input>
- Receive the click event from the button @ owner.PSCasementButtonBuild
> route

[route] Registered handler path <process>
- Continue through the implementation owned by the event handler @ owner.PSCasementButtonBuild
> output

[output] UI state after the event <output>
= The click event has completed through the current implementation.
! Code binding: cs|PSShared/PSCasement.cs|PSCasementButtonBuild|pButton|Click|1
