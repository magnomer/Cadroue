@format 1
@id ui-event.2d5aa46c60b92336
@title Viewfinder — drag change
@section I. UI
@area UI event
@tabs Split, Edit, Convert, Audio, Merge, Funnel
@event-ref cs|PFlow/PFlow.cs|PFlow|pViewfinder|PViewfinderDragChange|1
@entry input
@summary Handle the drag change event emitted by Viewfinder through the exact route registered in PFlow.PFlow.
@tag drag change
@tag PFlow
@alias owner = Cadroue.UIShell.PFlow.PFlow

[input] UI event <input>
- Receive the drag change event from Viewfinder through its current registration @ owner.PFlow
> route

[route] Registered handler path <process>
- Invoke the registered owner handler @ owner.PFlowDragSet
! Registered call path: PFlowDragSet
> output

[output] UI state after the event <output>
= The drag change event has completed through the current implementation.
! Code binding: cs|PFlow/PFlow.cs|PFlow|pViewfinder|PViewfinderDragChange|1
