@format 1
@id ui-event.93a9f21067f484c4
@title Viewfinder — section select
@section I. UI
@area UI event
@tabs Split, Edit, Convert, Audio, Merge, Funnel
@event-ref cs|PFlow/PFlow.cs|PFlow|pViewfinder|PViewfinderSectionSelect|1
@entry input
@summary Handle the section select event emitted by Viewfinder through the exact route registered in PFlow.PFlow.
@tag section select
@tag PFlow
@alias owner = Cadroue.UIShell.PFlow.PFlow

[input] UI event <input>
- Receive the section select event from Viewfinder through its current registration @ owner.PFlow
> route

[route] Registered handler path <process>
- Invoke the registered owner handler @ owner.PFlowViewfinderSelect
! Registered call path: PFlowViewfinderSelect
> output

[output] UI state after the event <output>
= The section select event has completed through the current implementation.
! Code binding: cs|PFlow/PFlow.cs|PFlow|pViewfinder|PViewfinderSectionSelect|1
