@format 1
@id ui-event.2c4263160beb95ca
@title Divider thumb — mouse left button down
@section I. UI
@area UI event
@tabs Split, Edit, Convert, Audio, Merge, Funnel
@event-ref cs|PFlow/PFlow.cs|PFlow|pDividerThumb|MouseLeftButtonDown|1
@entry input
@summary Handle the mouse left button down event exposed by PFlow.PFlow and follow its registered code path.
@tag mouse left button down
@tag PFlow
@alias owner = Cadroue.UIShell.PFlow.PFlow

[input] UI event <input>
- Receive the mouse left button down event from the divider thumb @ owner.PFlow
> route

[route] Registered handler path <process>
- Handle divider press @ owner.PDividerPressHandle
- Evaluate the handler's implemented conditional branches @ owner.PDividerPressHandle
- Mark the routed event handled on the implemented paths @ owner.PDividerPressHandle
> output

[output] UI state after the event <output>
= The mouse left button down event has completed through the current implementation.
! Code binding: cs|PFlow/PFlow.cs|PFlow|pDividerThumb|MouseLeftButtonDown|1
