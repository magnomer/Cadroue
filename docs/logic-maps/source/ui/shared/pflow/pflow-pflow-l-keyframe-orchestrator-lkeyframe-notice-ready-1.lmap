@format 1
@id ui-event.a5f0ca9ab848b582
@title Keyframe Orchestrator — keyframe notice ready
@section I. UI
@area UI event
@tabs Split, Edit, Convert, Audio, Merge, Funnel
@event-ref cs|PFlow/PFlow.cs|PFlow|lKeyframeOrchestrator|LKeyframeNoticeReady|1
@entry input
@summary Handle the keyframe notice ready event emitted by Keyframe Orchestrator through the exact route registered in PFlow.PFlow.
@tag keyframe notice ready
@tag PFlow
@alias owner = Cadroue.UIShell.PFlow.PFlow

[input] UI event <input>
- Receive the keyframe notice ready event from Keyframe Orchestrator through its current registration @ owner.PFlow
> route

[route] Registered handler path <process>
- Invoke the registered owner handler @ owner.PFlowNoticeHandle
! Registered call path: PFlowNoticeHandle
> output

[output] UI state after the event <output>
= The keyframe notice ready event has completed through the current implementation.
! Code binding: cs|PFlow/PFlow.cs|PFlow|lKeyframeOrchestrator|LKeyframeNoticeReady|1
