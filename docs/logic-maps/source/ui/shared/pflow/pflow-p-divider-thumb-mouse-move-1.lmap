@format 1
@id ui-event.94a73144a50baccd
@title Divider thumb — mouse move
@section I. UI
@area UI event
@tabs Split, Edit, Convert, Audio, Merge, Funnel
@event-ref cs|PFlow/PFlow.cs|PFlow|pDividerThumb|MouseMove|1
@entry input
@summary Handle the mouse move event exposed by PFlow.PFlow and follow its registered code path.
@tag mouse move
@tag PFlow
@alias owner = Cadroue.UIShell.PFlow.PFlow

[input] UI event <input>
- Receive the mouse move event from the divider thumb @ owner.PFlow
> route

[route] Registered handler path <process>
- Handle divider move @ owner.PDividerMoveHandle
- Clear divider @ owner.PDividerClear
- Evaluate the handler's implemented conditional branches @ owner.PDividerMoveHandle
- Mark the routed event handled on the implemented paths @ owner.PDividerMoveHandle
> output

[output] UI state after the event <output>
= The mouse move event has completed through the current implementation.
! Code binding: cs|PFlow/PFlow.cs|PFlow|pDividerThumb|MouseMove|1
