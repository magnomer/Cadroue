@format 1
@id ui-event.6cd305b9d7a1271e
@title Viewfinder — cursor change
@section I. UI
@area UI event
@tabs Split, Edit, Convert, Audio, Merge, Funnel
@event-ref cs|PFlow/PFlow.cs|PFlow|pViewfinder|PViewfinderCursorChange|1
@entry input
@summary Handle the cursor change event emitted by Viewfinder through the exact route registered in PFlow.PFlow.
@tag cursor change
@tag PFlow
@alias owner = Cadroue.UIShell.PFlow.PFlow

[input] UI event <input>
- Receive the cursor change event from Viewfinder through its current registration @ owner.PFlow
> route

[route] Registered handler path <process>
- Invoke the registered owner handler @ owner.PFlowViewfinderSeek
! Registered call path: PFlowViewfinderSeek
> output

[output] UI state after the event <output>
= The cursor change event has completed through the current implementation.
! Code binding: cs|PFlow/PFlow.cs|PFlow|pViewfinder|PViewfinderCursorChange|1
