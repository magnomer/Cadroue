@format 1
@id ui-event.0879f345c4bc613b
@title Divider thumb — lost mouse capture
@section I. UI
@area UI event
@tabs Split, Edit, Convert, Audio, Merge, Funnel
@event-ref cs|PFlow/PFlow.cs|PFlow|pDividerThumb|LostMouseCapture|1
@entry input
@summary Handle the lost mouse capture event exposed by PFlow.PFlow and follow its registered code path.
@tag lost mouse capture
@tag PFlow
@alias owner = Cadroue.UIShell.PFlow.PFlow

[input] UI event <input>
- Receive the lost mouse capture event from the divider thumb @ owner.PFlow
> route

[route] Registered handler path <process>
- Handle divider capture @ owner.PDividerCaptureHandle
- Clear divider @ owner.PDividerClear
> output

[output] UI state after the event <output>
= The lost mouse capture event has completed through the current implementation.
! Code binding: cs|PFlow/PFlow.cs|PFlow|pDividerThumb|LostMouseCapture|1
