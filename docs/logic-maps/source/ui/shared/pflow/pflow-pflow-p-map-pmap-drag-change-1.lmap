@format 1
@id ui-event.422e288f5120571e
@title Map — drag change
@section I. UI
@area UI event
@tabs Split, Edit, Convert, Audio, Merge, Funnel
@event-ref cs|PFlow/PFlow.cs|PFlow|pMap|PMapDragChange|1
@entry input
@summary Handle the drag change event emitted by Map through the exact route registered in PFlow.PFlow.
@tag drag change
@tag PFlow
@alias owner = Cadroue.UIShell.PFlow.PFlow

[input] UI event <input>
- Receive the drag change event from Map through its current registration @ owner.PFlow
> route

[route] Registered handler path <process>
- Invoke the registered owner handler @ owner.PFlowDragSet
! Registered call path: PFlowDragSet
> output

[output] UI state after the event <output>
= The drag change event has completed through the current implementation.
! Code binding: cs|PFlow/PFlow.cs|PFlow|pMap|PMapDragChange|1
