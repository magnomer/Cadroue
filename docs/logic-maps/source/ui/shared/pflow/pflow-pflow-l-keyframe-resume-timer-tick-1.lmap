@format 1
@id ui-event.266696b50c7eeb44
@title Keyframe Resume Timer — tick
@section I. UI
@area UI event
@tabs Split, Edit, Convert, Audio, Merge, Funnel
@event-ref cs|PFlow/PFlow.cs|PFlow|lKeyframeResumeTimer|Tick|1
@entry input
@summary Handle the tick event emitted by Keyframe Resume Timer through the exact route registered in PFlow.PFlow.
@tag tick
@tag PFlow
@alias owner = Cadroue.UIShell.PFlow.PFlow

[input] UI event <input>
- Receive the tick event from Keyframe Resume Timer through its current registration @ owner.PFlow
> route

[route] Registered handler path <process>
- Invoke the registered owner handler @ owner.PFlowResumeHandle
! Registered call path: PFlowResumeHandle
> output

[output] UI state after the event <output>
= The tick event has completed through the current implementation.
! Code binding: cs|PFlow/PFlow.cs|PFlow|lKeyframeResumeTimer|Tick|1
