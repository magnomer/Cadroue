@format 1
@id ui-event.50151b457d7d99d6
@title Map — cursor change
@section I. UI
@area UI event
@tabs Split, Edit, Convert, Audio, Merge, Funnel
@event-ref cs|PFlow/PFlow.cs|PFlow|pMap|PMapCursorChange|1
@entry input
@summary Handle the cursor change event emitted by Map through the exact route registered in PFlow.PFlow.
@tag cursor change
@tag PFlow
@alias owner = Cadroue.UIShell.PFlow.PFlow

[input] UI event <input>
- Receive the cursor change event from Map through its current registration @ owner.PFlow
> route

[route] Registered handler path <process>
- Invoke the registered owner handler @ owner.PFlowMapSeek
! Registered call path: PFlowMapSeek
> output

[output] UI state after the event <output>
= The cursor change event has completed through the current implementation.
! Code binding: cs|PFlow/PFlow.cs|PFlow|pMap|PMapCursorChange|1
