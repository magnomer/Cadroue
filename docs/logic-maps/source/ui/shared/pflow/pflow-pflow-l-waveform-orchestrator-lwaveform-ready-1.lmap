@format 1
@id ui-event.8e2b4d55f77dee81
@title Waveform Orchestrator — waveform ready
@section I. UI
@area UI event
@tabs Split, Edit, Convert, Audio, Merge, Funnel
@event-ref cs|PFlow/PFlow.cs|PFlow|lWaveformOrchestrator|LWaveformReady|1
@entry input
@summary Handle the waveform ready event emitted by Waveform Orchestrator through the exact route registered in PFlow.PFlow.
@tag waveform ready
@tag PFlow
@alias owner = Cadroue.UIShell.PFlow.PFlow

[input] UI event <input>
- Receive the waveform ready event from Waveform Orchestrator through its current registration @ owner.PFlow
> route

[route] Registered handler path <process>
- Invoke the registered owner handler @ owner.PFlowWaveformHandle
! Registered call path: PFlowWaveformHandle
> output

[output] UI state after the event <output>
= The waveform ready event has completed through the current implementation.
! Code binding: cs|PFlow/PFlow.cs|PFlow|lWaveformOrchestrator|LWaveformReady|1
