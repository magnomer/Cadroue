@format 1
@id ui-event.7cbb462e555560dc
@title Divider thumb — mouse left button up
@section I. UI
@area UI event
@tabs Split, Edit, Convert, Audio, Merge, Funnel
@event-ref cs|PFlow/PFlow.cs|PFlow|pDividerThumb|MouseLeftButtonUp|1
@entry input
@summary Handle the mouse left button up event exposed by PFlow.PFlow and follow its registered code path.
@tag mouse left button up
@tag PFlow
@alias owner = Cadroue.UIShell.PFlow.PFlow

[input] UI event <input>
- Receive the mouse left button up event from the divider thumb @ owner.PFlow
> route

[route] Registered handler path <process>
- Handle divider release @ owner.PDividerReleaseHandle
- Clear divider @ owner.PDividerClear
- Mark the routed event handled on the implemented paths @ owner.PDividerReleaseHandle
> output

[output] UI state after the event <output>
= The mouse left button up event has completed through the current implementation.
! Code binding: cs|PFlow/PFlow.cs|PFlow|pDividerThumb|MouseLeftButtonUp|1
