@format 1
@id ui-event.246b44d5417d8f99
@title Row border — mouse left button down
@section I. UI
@area UI event
@tabs Split, Edit, Convert, Audio, Merge, Funnel
@event-ref cs|PPanels/PList.cs|PListRowBuild|pRowBorder|MouseLeftButtonDown|1
@entry input
@summary Handle the mouse left button down event exposed by PList.PListRowBuild and follow its registered code path.
@tag mouse left button down
@tag PList
@alias owner = Cadroue.UIShell.PPanels.PList

[input] UI event <input>
- Receive the mouse left button down event from the row border @ owner.PListRowBuild
> route

[route] Registered handler path <process>
- Handle list press @ owner.PListPressHandle
- Apply list range @ owner.PListRangeApply
- Toggle list selection @ owner.PListSelectionToggle
- Check list selection @ owner.PListSelectionCheck
- Apply list current @ owner.PListCurrentApply
- Apply list select @ owner.PListSelectApply
- Arm mouse capture and drag tracking only when the pressed row is unlocked @ owner.PListRowBuild
- Evaluate the handler's implemented conditional branches @ owner.PListPressHandle
- Mark the routed event handled on the implemented paths @ owner.PListPressHandle
> output

[output] UI state after the event <output>
= Selection and current-row state are updated for any row, while locked submitted rows cannot begin a drag.
! Code binding: cs|PPanels/PList.cs|PListRowBuild|pRowBorder|MouseLeftButtonDown|1
