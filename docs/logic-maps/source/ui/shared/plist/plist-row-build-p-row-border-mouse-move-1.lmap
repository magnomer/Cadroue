@format 1
@id ui-event.1cd56c24683b5c4d
@title Row border — mouse move
@section I. UI
@area UI event
@tabs Split, Edit, Convert, Audio, Merge, Funnel
@event-ref cs|PPanels/PList.cs|PListRowBuild|pRowBorder|MouseMove|1
@entry input
@summary Handle the mouse move event exposed by PList.PListRowBuild and follow its registered code path.
@tag mouse move
@tag PList
@alias owner = Cadroue.UIShell.PPanels.PList

[input] UI event <input>
- Receive the mouse move event from the row border @ owner.PListRowBuild
> route

[route] Registered handler path <process>
- Handle list drag @ owner.PListDragHandle
- Check list selection @ owner.PListSelectionCheck
- Read list selection @ owner.PListSelectionRead
- Remove locked submitted paths from a multi-row drag and cancel when none remain @ owner.PListPathLockedCheck
- Evaluate the handler's implemented conditional branches @ owner.PListDragHandle
> output

[output] UI state after the event <output>
= A drag contains only unlocked paths; a locked-only selection produces no drag operation.
! Code binding: cs|PPanels/PList.cs|PListRowBuild|pRowBorder|MouseMove|1
