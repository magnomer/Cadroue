@format 1
@id ui-event.678ed7e3dc54b9b3
@title Row border — mouse left button up
@section I. UI
@area UI event
@tabs Split, Edit, Convert, Audio, Merge, Funnel
@event-ref cs|PPanels/PList.cs|PListRowBuild|pRowBorder|MouseLeftButtonUp|1
@entry input
@summary Handle the mouse left button up event exposed by PList.PListRowBuild and follow its registered code path.
@tag mouse left button up
@tag PList
@alias owner = Cadroue.UIShell.PPanels.PList

[input] UI event <input>
- Receive the mouse left button up event from the row border @ owner.PListRowBuild
> route

[route] Registered handler path <process>
- Handle list release @ owner.PListReleaseHandle
- Apply list select @ owner.PListSelectApply
- Evaluate the handler's implemented conditional branches @ owner.PListReleaseHandle
> output

[output] UI state after the event <output>
= The mouse left button up event has completed through the current implementation.
! Code binding: cs|PPanels/PList.cs|PListRowBuild|pRowBorder|MouseLeftButtonUp|1
