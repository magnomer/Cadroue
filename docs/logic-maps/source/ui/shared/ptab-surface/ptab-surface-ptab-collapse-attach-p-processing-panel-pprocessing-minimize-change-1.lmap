@format 1
@id ui-event.953a876a2a52d44c
@title Processing Panel — processing minimize change
@section I. UI
@area UI event
@tabs Split, Edit, Convert, Audio, Merge, Funnel
@event-ref cs|PMainArea/PTabSurface.cs|PTabCollapseAttach|pProcessingPanel|PProcessingMinimizeChange|1
@entry input
@summary Handle the processing minimize change event emitted by Processing Panel through the exact route registered in PTabSurface.PTabCollapseAttach.
@tag processing minimize change
@tag PTabSurface
@alias owner = Cadroue.UIShell.PMainArea.PTabSurface

[input] UI event <input>
- Receive the processing minimize change event from Processing Panel through its current registration @ owner.PTabCollapseAttach
> route

[route] Registered handler path <process>
- Invoke the registered owner handler @ owner.pCollapseApply
! Registered call path: pCollapseApply
> output

[output] UI state after the event <output>
= The processing minimize change event has completed through the current implementation.
! Code binding: cs|PMainArea/PTabSurface.cs|PTabCollapseAttach|pProcessingPanel|PProcessingMinimizeChange|1
