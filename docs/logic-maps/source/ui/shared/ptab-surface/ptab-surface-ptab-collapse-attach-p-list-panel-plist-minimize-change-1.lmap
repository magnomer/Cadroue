@format 1
@id ui-event.3402862fc8cd6963
@title List Panel — list minimize change
@section I. UI
@area UI event
@tabs Split, Edit, Convert, Audio, Merge, Funnel
@event-ref cs|PMainArea/PTabSurface.cs|PTabCollapseAttach|pListPanel|PListMinimizeChange|1
@entry input
@summary Handle the list minimize change event emitted by List Panel through the exact route registered in PTabSurface.PTabCollapseAttach.
@tag list minimize change
@tag PTabSurface
@alias owner = Cadroue.UIShell.PMainArea.PTabSurface

[input] UI event <input>
- Receive the list minimize change event from List Panel through its current registration @ owner.PTabCollapseAttach
> route

[route] Registered handler path <process>
- Invoke the registered owner handler @ owner.pCollapseApply
! Registered call path: pCollapseApply
> output

[output] UI state after the event <output>
= The list minimize change event has completed through the current implementation.
! Code binding: cs|PMainArea/PTabSurface.cs|PTabCollapseAttach|pListPanel|PListMinimizeChange|1
