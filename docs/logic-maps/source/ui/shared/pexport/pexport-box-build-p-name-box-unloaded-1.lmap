@format 1
@id ui-event.436522f3c112e2bd
@title Name box — unloaded
@section I. UI
@area UI event
@tabs Split, Edit, Convert, Audio, Merge
@event-ref cs|PPanels/PExportPreset.cs|PExportBoxBuild|pNameBox|Unloaded|1
@entry input
@summary Handle the unloaded event exposed by PExport.PExportBoxBuild and follow its registered code path.
@tag unloaded
@tag PExport
@alias owner = Cadroue.UIShell.PPanels.PExport

[input] UI event <input>
- Receive the unloaded event from the name box @ owner.PExportBoxBuild
> route

[route] Registered handler path <process>
- Evaluate the handler's implemented conditional branches @ owner.PExportBoxBuild
- Mark the routed event handled on the implemented paths @ owner.PExportBoxBuild
> output

[output] UI state after the event <output>
= The unloaded event has completed through the current implementation.
! Code binding: cs|PPanels/PExportPreset.cs|PExportBoxBuild|pNameBox|Unloaded|1
