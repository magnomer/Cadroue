@format 1
@id ui-event.a3777f583455c910
@title Tabs — selection changed
@section I. UI
@area UI event
@tabs Split, Edit, Convert, Audio, Merge, Global interface
@event-ref cs|PSShared/PSSheet.cs|PSSheetControlBuild|pTabs|SelectionChanged|1
@entry input
@summary Handle the selection changed event exposed by PSSheet.PSSheetControlBuild and follow its registered code path.
@tag selection changed
@tag PSSheet
@alias owner = Cadroue.UIShell.PSShared.PSSheet

[input] UI event <input>
- Receive the selection changed event from the tabs @ owner.PSSheetControlBuild
> route

[route] Registered handler path <process>
- Update ps sheet separator @ owner.PSSheetSeparatorUpdate
- Evaluate the handler's implemented conditional branches @ owner.PSSheetSeparatorUpdate
- Update the visual or selection state owned by the handler @ owner.PSSheetSeparatorUpdate
> output

[output] UI state after the event <output>
= The selection changed event has completed through the current implementation.
! Code binding: cs|PSShared/PSSheet.cs|PSSheetControlBuild|pTabs|SelectionChanged|1
