@format 1
@id ui-event.9488b39f66e16895
@title Flow — waveform change
@section I. UI
@area UI event
@tabs Split, Edit, Convert, Audio, Merge, Funnel
@event-ref cs|PMainArea/PCompass.cs|PCompass|pFlow|PFlowWaveformChange|1
@entry input
@summary Handle the waveform change event emitted by Flow through the exact route registered in PCompass.PCompass.
@tag waveform change
@tag PCompass
@alias owner = Cadroue.UIShell.PMainArea.PCompass

[input] UI event <input>
- Receive the waveform change event from Flow through its current registration @ owner.PCompass
> route

[route] Registered handler path <process>
- Invoke the registered owner handler @ owner.PCompassWaveformApply
! Registered call path: PCompassWaveformApply
> output

[output] UI state after the event <output>
= The waveform change event has completed through the current implementation.
! Code binding: cs|PMainArea/PCompass.cs|PCompass|pFlow|PFlowWaveformChange|1
