@format 1
@id ui-event.7b8bff5c95096fd2
@title Compass volume slider — value changed
@section I. UI
@area UI event
@tabs Split, Edit, Convert, Audio, Merge, Funnel
@event-ref cs|PMainArea/PCompass.cs|PCompass|pCompassVolumeSlider|ValueChanged|1
@entry input
@summary Handle the value changed event exposed by PCompass.PCompass and follow its registered code path.
@tag value changed
@tag PCompass
@alias owner = Cadroue.UIShell.PMainArea.PCompass

[input] UI event <input>
- Receive the value changed event from the compass volume slider @ owner.PCompass
> route

[route] Registered handler path <process>
- Handle compass volume @ owner.PCompassVolumeHandle
- Update compass track @ owner.PCompassTrackUpdate
- Evaluate the handler's implemented conditional branches @ owner.PCompassVolumeHandle
- Update the visual or selection state owned by the handler @ owner.PCompassVolumeHandle
> output

[output] UI state after the event <output>
= The value changed event has completed through the current implementation.
! Code binding: cs|PMainArea/PCompass.cs|PCompass|pCompassVolumeSlider|ValueChanged|1
