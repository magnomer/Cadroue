@format 1
@id ui-event.a1a300a72de87441
@title Button — click
@section I. UI
@area UI event
@tabs Split, Edit, Convert, Audio, Merge, Funnel
@event-ref cs|PMainArea/PCompass.cs|PCompassWaveformBuild|pButton|Click|1
@entry input
@summary Handle the click event exposed by PCompass.PCompassWaveformBuild and follow its registered code path.
@tag click
@tag PCompass
@alias owner = Cadroue.UIShell.PMainArea.PCompass

[input] UI event <input>
- Receive the click event from the button @ owner.PCompassWaveformBuild
> route

[route] Registered handler path <process>
- Update the visual or selection state owned by the handler @ owner.PCompassWaveformBuild
> output

[output] UI state after the event <output>
= The click event has completed through the current implementation.
! Code binding: cs|PMainArea/PCompass.cs|PCompassWaveformBuild|pButton|Click|1
