@format 1
@id ui-event.32da116083a4918d
@title Flow — volume value
@section I. UI
@area UI event
@tabs Split, Edit, Convert, Audio, Merge, Funnel
@event-ref cs|PMainArea/PCompass.cs|PCompass|pFlow|PFlowVolumeValue|1
@entry input
@summary Handle the volume value event emitted by Flow through the exact route registered in PCompass.PCompass.
@tag volume value
@tag PCompass
@alias owner = Cadroue.UIShell.PMainArea.PCompass

[input] UI event <input>
- Receive the volume value event from Flow through its current registration @ owner.PCompass
> route

[route] Registered handler path <process>
- Invoke the registered owner handler @ owner.PCompassValueHandle
! Registered call path: PCompassValueHandle
> output

[output] UI state after the event <output>
= The volume value event has completed through the current implementation.
! Code binding: cs|PMainArea/PCompass.cs|PCompass|pFlow|PFlowVolumeValue|1
