@format 1
@id ui-event.316a1f824d46059e
@title Viewer overlay — mouse move
@section I. UI
@area UI event
@tabs Split, Edit, Convert, Audio, Merge, Funnel
@event-ref cs|PPanels/PViewer.cs|PViewer|pViewerOverlay|MouseMove|1
@entry input
@summary Handle the mouse move event exposed by PViewer.PViewer and follow its registered code path.
@tag mouse move
@tag PViewer
@alias owner = Cadroue.UIShell.PPanels.PViewer

[input] UI event <input>
- Receive the mouse move event from the viewer overlay @ owner.PViewer
> route

[route] Registered handler path <process>
- Handle crop move @ owner.PCropMoveHandle
- Apply crop drag @ owner.PCropDragApply
- Execute crop box place @ owner.PCropBoxPlace
- Evaluate the handler's implemented conditional branches @ owner.PCropMoveHandle
- Mark the routed event handled on the implemented paths @ owner.PCropMoveHandle
> output

[output] UI state after the event <output>
= The mouse move event has completed through the current implementation.
! Code binding: cs|PPanels/PViewer.cs|PViewer|pViewerOverlay|MouseMove|1
