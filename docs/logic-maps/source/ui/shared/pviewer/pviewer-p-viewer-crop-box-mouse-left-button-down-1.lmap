@format 1
@id ui-event.92eafaa9dd94879c
@title Viewer crop box — mouse left button down
@section I. UI
@area UI event
@tabs Split, Edit, Convert, Audio, Merge, Funnel
@event-ref cs|PPanels/PViewer.cs|PViewer|pViewerCropBox|MouseLeftButtonDown|1
@entry input
@summary Handle the mouse left button down event exposed by PViewer.PViewer and follow its registered code path.
@tag mouse left button down
@tag PViewer
@alias owner = Cadroue.UIShell.PPanels.PViewer

[input] UI event <input>
- Receive the mouse left button down event from the viewer crop box @ owner.PViewer
> route

[route] Registered handler path <process>
- Handle crop body @ owner.PCropBodyHandle
- Start crop drag @ owner.PCropDragStart
- Evaluate the handler's implemented conditional branches @ owner.PCropBodyHandle
- Mark the routed event handled on the implemented paths @ owner.PCropBodyHandle
- Update the visual or selection state owned by the handler @ owner.PCropBodyHandle
> output

[output] UI state after the event <output>
= The mouse left button down event has completed through the current implementation.
! Code binding: cs|PPanels/PViewer.cs|PViewer|pViewerCropBox|MouseLeftButtonDown|1
