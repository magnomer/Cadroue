@format 1
@id ui-event.15e83980bde0a65f
@title Viewer Clock Timer — tick
@section I. UI
@area UI event
@tabs Split, Edit, Convert, Audio, Merge, Funnel
@event-ref cs|PPanels/PViewer.cs|PViewer|pViewerClockTimer|Tick|1
@entry input
@summary Handle the tick event emitted by Viewer Clock Timer through the exact route registered in PViewer.PViewer.
@tag tick
@tag PViewer
@alias owner = Cadroue.UIShell.PPanels.PViewer

[input] UI event <input>
- Receive the tick event from Viewer Clock Timer through its current registration @ owner.PViewer
> route

[route] Registered handler path <process>
- Invoke the registered owner handler @ owner.PViewerClockHandle
! Registered call path: PViewerClockHandle
> output

[output] UI state after the event <output>
= The tick event has completed through the current implementation.
! Code binding: cs|PPanels/PViewer.cs|PViewer|pViewerClockTimer|Tick|1
