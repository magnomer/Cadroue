@format 1
@id ui-event.0fcb303240d4c05d
@title Processing row panel — preview mouse move
@section I. UI
@area UI event
@tabs Edit, Audio
@event-ref cs|PPanels/PProcessing.cs|PProcessing|pProcessingRowPanel|PreviewMouseMove|1
@entry input
@summary Handle the preview mouse move event exposed by PProcessing.PProcessing and follow its registered code path.
@tag preview mouse move
@tag PProcessing
@alias owner = Cadroue.UIShell.PPanels.PProcessing

[input] UI event <input>
- Receive the preview mouse move event from the processing row panel @ owner.PProcessing
> route

[route] Registered handler path <process>
- Handle processing move @ owner.PProcessingMoveHandle
- Resolve processing index @ owner.PProcessingIndexResolve
- Update processing numbers @ owner.PProcessingNumbersUpdate
- Evaluate the handler's implemented conditional branches @ owner.PProcessingMoveHandle
- Update the visual or selection state owned by the handler @ owner.PProcessingMoveHandle
> output

[output] UI state after the event <output>
= The preview mouse move event has completed through the current implementation.
! Code binding: cs|PPanels/PProcessing.cs|PProcessing|pProcessingRowPanel|PreviewMouseMove|1
