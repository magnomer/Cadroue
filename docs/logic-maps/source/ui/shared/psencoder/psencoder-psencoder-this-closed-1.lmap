@format 1
@id ui-event.33c638c9d12810b4
@title S Encoder — closed
@section I. UI
@area UI event
@tabs Split, Edit, Convert, Audio, Merge
@event-ref cs|PPanels/PSEncoder.cs|PSEncoder|this|Closed|1
@entry input
@summary Handle the closed event emitted by S Encoder through the exact route registered in PSEncoder.PSEncoder.
@tag closed
@tag PSEncoder
@alias owner = Cadroue.UIShell.PPanels.PSEncoder

[input] UI event <input>
- Receive the closed event from S Encoder through its current registration @ owner.PSEncoder
> route

[route] Registered handler path <process>
- Invoke the registered owner handler @ owner.PSEncoderCloseHandle
! Registered call path: PSEncoderCloseHandle
> output

[output] UI state after the event <output>
= The closed event has completed through the current implementation.
! Code binding: cs|PPanels/PSEncoder.cs|PSEncoder|this|Closed|1
