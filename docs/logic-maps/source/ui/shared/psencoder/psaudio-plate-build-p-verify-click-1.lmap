@format 1
@id ui-event.8e7ab211be439c36
@title Verify — click
@section I. UI
@area UI event
@tabs Split, Edit, Convert, Audio, Merge
@event-ref cs|PPanels/PSEncoderAudio.cs|PSAudioPlateBuild|pVerify|Click|1
@entry input
@summary Handle the click event exposed by PSEncoder.PSAudioPlateBuild and follow its registered code path.
@tag click
@tag PSEncoder
@alias owner = Cadroue.UIShell.PPanels.PSEncoder

[input] UI event <input>
- Receive the click event from the verify @ owner.PSAudioPlateBuild
> route

[route] Registered handler path <process>
- Handle ps audio verify @ owner.PSAudioVerifyHandle
- Execute ps verdict log record @ owner.PSVerdictLogRecord
- Evaluate the handler's implemented conditional branches @ owner.PSAudioVerifyHandle
- Update the visual or selection state owned by the handler @ owner.PSAudioVerifyHandle
> output

[output] UI state after the event <output>
= The click event has completed through the current implementation.
! Code binding: cs|PPanels/PSEncoderAudio.cs|PSAudioPlateBuild|pVerify|Click|1
