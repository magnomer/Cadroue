@format 1
@id ui-event.dc87e1ff76a33e35
@title Border — preview mouse left button down
@section I. UI
@area UI event
@tabs Split, Edit, Convert, Audio, Merge
@event-ref cs|PPanels/PSEncoderOutput.cs|PSNameTokenBuild|pBorder|PreviewMouseLeftButtonDown|1
@entry input
@summary Handle the preview mouse left button down event exposed by PSEncoder.PSNameTokenBuild and follow its registered code path.
@tag preview mouse left button down
@tag PSEncoder
@alias owner = Cadroue.UIShell.PPanels.PSEncoder

[input] UI event <input>
- Receive the preview mouse left button down event from the border @ owner.PSNameTokenBuild
> route

[route] Registered handler path <process>
- Evaluate the handler's implemented conditional branches @ owner.PSNameTokenBuild
- Update the visual or selection state owned by the handler @ owner.PSNameTokenBuild
> output

[output] UI state after the event <output>
= The preview mouse left button down event has completed through the current implementation.
! Code binding: cs|PPanels/PSEncoderOutput.cs|PSNameTokenBuild|pBorder|PreviewMouseLeftButtonDown|1
