@format 1
@id ui-event.1264380a7e6b9818
@title Border — mouse leave
@section I. UI
@area UI event
@tabs Split, Edit, Convert, Audio, Merge
@event-ref cs|PPanels/PSEncoderOutput.cs|PSNameTokenBuild|pBorder|MouseLeave|1
@entry input
@summary Handle the mouse leave event exposed by PSEncoder.PSNameTokenBuild and follow its registered code path.
@tag mouse leave
@tag PSEncoder
@alias owner = Cadroue.UIShell.PPanels.PSEncoder

[input] UI event <input>
- Receive the mouse leave event from the border @ owner.PSNameTokenBuild
> route

[route] Registered handler path <process>
- Evaluate the handler's implemented conditional branches @ owner.PSNameTokenBuild
- Update the visual or selection state owned by the handler @ owner.PSNameTokenBuild
> output

[output] UI state after the event <output>
= The mouse leave event has completed through the current implementation.
! Code binding: cs|PPanels/PSEncoderOutput.cs|PSNameTokenBuild|pBorder|MouseLeave|1
