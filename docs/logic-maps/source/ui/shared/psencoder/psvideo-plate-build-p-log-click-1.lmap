@format 1
@id ui-event.7c83be804e848480
@title Log — click
@section I. UI
@area UI event
@tabs Split, Edit, Convert, Audio, Merge
@event-ref cs|PPanels/PSEncoderVideo.cs|PSVideoPlateBuild|pLog|Click|1
@entry input
@summary Handle the click event exposed by PSEncoder.PSVideoPlateBuild and follow its registered code path.
@tag click
@tag PSEncoder
@alias owner = Cadroue.UIShell.PPanels.PSEncoder

[input] UI event <input>
- Receive the click event from the log @ owner.PSVideoPlateBuild
> route

[route] Registered handler path <process>
- Update the visual or selection state owned by the handler @ owner.PSVideoPlateBuild
> output

[output] UI state after the event <output>
= The click event has completed through the current implementation.
! Code binding: cs|PPanels/PSEncoderVideo.cs|PSVideoPlateBuild|pLog|Click|1
