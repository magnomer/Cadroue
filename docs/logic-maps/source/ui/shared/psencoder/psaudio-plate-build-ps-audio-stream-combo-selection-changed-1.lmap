@format 1
@id ui-event.c0f4c50be4a7b631
@title Audio stream combo — selection changed
@section I. UI
@area UI event
@tabs Split, Edit, Convert, Audio, Merge
@event-ref cs|PPanels/PSEncoderAudio.cs|PSAudioPlateBuild|psAudioStreamCombo|SelectionChanged|1
@entry input
@summary Handle the selection changed event exposed by PSEncoder.PSAudioPlateBuild and follow its registered code path.
@tag selection changed
@tag PSEncoder
@alias owner = Cadroue.UIShell.PPanels.PSEncoder

[input] UI event <input>
- Receive the selection changed event from the audio stream combo @ owner.PSAudioPlateBuild
> route

[route] Registered handler path <process>
- Update ps audio scope @ owner.PSAudioScopeUpdate
- Update the visual or selection state owned by the handler @ owner.PSAudioScopeUpdate
> output

[output] UI state after the event <output>
= The selection changed event has completed through the current implementation.
! Code binding: cs|PPanels/PSEncoderAudio.cs|PSAudioPlateBuild|psAudioStreamCombo|SelectionChanged|1
