@format 1
@id ui-event.ae7bf78c83a7dc6a
@title Border — mouse enter
@section I. UI
@area UI event
@tabs Split, Edit, Convert, Audio, Merge
@event-ref cs|PPanels/PSEncoderOutput.cs|PSNameTokenBuild|pBorder|MouseEnter|1
@entry input
@summary Handle the mouse enter event exposed by PSEncoder.PSNameTokenBuild and follow its registered code path.
@tag mouse enter
@tag PSEncoder
@alias owner = Cadroue.UIShell.PPanels.PSEncoder

[input] UI event <input>
- Receive the mouse enter event from the border @ owner.PSNameTokenBuild
> route

[route] Registered handler path <process>
- Evaluate the handler's implemented conditional branches @ owner.PSNameTokenBuild
- Update the visual or selection state owned by the handler @ owner.PSNameTokenBuild
> output

[output] UI state after the event <output>
= The mouse enter event has completed through the current implementation.
! Code binding: cs|PPanels/PSEncoderOutput.cs|PSNameTokenBuild|pBorder|MouseEnter|1
