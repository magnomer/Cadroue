@format 1
@id ui-event.1629d77b81a5827d
@title Viewfinder — mouse move
@section I. UI
@area UI event
@tabs Split, Edit, Convert, Audio, Merge, Funnel
@event-ref override|PFlow/PViewfinderInput.cs|OnMouseMove
@entry input
@summary Handle the mouse move event exposed by PViewfinder.OnMouseMove and follow its registered code path.
@tag mouse move
@tag PViewfinder
@alias owner = Cadroue.UIShell.PFlow.PViewfinder

[input] UI event <input>
- Receive the framework mouse move callback @ owner.OnMouseMove
> route

[route] Registered handler path <process>
- Resolve viewfinder position @ owner.PViewfinderPositionResolve
- Evaluate the handler's implemented conditional branches @ owner.OnMouseMove
- Mark the routed event handled on the implemented paths @ owner.OnMouseMove
> output

[output] UI state after the event <output>
= The mouse move event has completed through the current implementation.
! Code binding: override|PFlow/PViewfinderInput.cs|OnMouseMove
