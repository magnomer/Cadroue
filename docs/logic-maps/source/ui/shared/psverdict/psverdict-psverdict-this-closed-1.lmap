@format 1
@id ui-event.b9107f65799bf06c
@title S Verdict — closed
@section I. UI
@area UI event
@tabs Split, Edit, Convert, Audio, Merge
@event-ref cs|PSVerdict/PSVerdict.cs|PSVerdict|this|Closed|1
@entry input
@summary Handle the closed event emitted by S Verdict through the exact route registered in PSVerdict.PSVerdict.
@tag closed
@tag PSVerdict
@alias owner = Cadroue.UIShell.PSVerdict

[input] UI event <input>
- Receive the closed event from S Verdict through its current registration @ owner.PSVerdict
> route

[route] Registered handler path <process>
- Invoke the registered owner handler @ owner.PSVerdictCloseHandle
! Registered call path: PSVerdictCloseHandle
> output

[output] UI state after the event <output>
= The closed event has completed through the current implementation.
! Code binding: cs|PSVerdict/PSVerdict.cs|PSVerdict|this|Closed|1
