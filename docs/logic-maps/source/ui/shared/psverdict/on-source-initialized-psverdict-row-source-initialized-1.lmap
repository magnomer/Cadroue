@format 1
@id ui-event.cdbe47bf0af966cd
@title Verdict — source initialized
@section I. UI
@area UI event
@tabs Split, Edit, Convert, Audio, Merge
@event-ref override|PSVerdict/PSVerdict.cs|OnSourceInitialized
@entry input
@summary Handle the source initialized event exposed by PSVerdict.OnSourceInitialized and follow its registered code path.
@tag source initialized
@tag PSVerdict
@alias owner = Cadroue.UIShell.PSVerdict

[input] UI event <input>
- Receive the framework source initialized callback @ owner.OnSourceInitialized
> route

[route] Registered handler path <process>
- Continue through the implementation owned by the event handler @ owner.OnSourceInitialized
> output

[output] UI state after the event <output>
= The source initialized event has completed through the current implementation.
! Code binding: override|PSVerdict/PSVerdict.cs|OnSourceInitialized
