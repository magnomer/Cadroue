@format 1
@id ui-event.3cf91d88ce4a0e51
@title Source path box — key down
@section I. UI
@area UI event
@tabs Split, Edit, Convert, Audio, Merge, Funnel
@event-ref cs|PPanels/PSource.cs|PSource|pSourcePathBox|KeyDown|1
@entry input
@summary Handle the key down event exposed by PSource.PSource and follow its registered code path.
@tag key down
@tag PSource
@alias owner = Cadroue.UIShell.PPanels.PSource

[input] UI event <input>
- Receive the key down event from the source path box @ owner.PSource
> route

[route] Registered handler path <process>
- Handle source key @ owner.PSourceKeyHandle
- Check source audio @ owner.PSourceAudioCheck
- Evaluate the handler's implemented conditional branches @ owner.PSourceKeyHandle
- Mark the routed event handled on the implemented paths @ owner.PSourceKeyHandle
> output

[output] UI state after the event <output>
= The key down event has completed through the current implementation.
! Code binding: cs|PPanels/PSource.cs|PSource|pSourcePathBox|KeyDown|1
