@format 1
@id ui-event.202f97380174f03c
@title Source path box — text changed
@section I. UI
@area UI event
@tabs Split, Edit, Convert, Audio, Merge, Funnel
@event-ref cs|PPanels/PSource.cs|PSource|pSourcePathBox|TextChanged|1
@entry input
@summary Handle the text changed event exposed by PSource.PSource and follow its registered code path.
@tag text changed
@tag PSource
@alias owner = Cadroue.UIShell.PPanels.PSource

[input] UI event <input>
- Receive the text changed event from the source path box @ owner.PSource
> route

[route] Registered handler path <process>
- Handle source text @ owner.PSourceTextHandle
- Execute source placeholder sync @ owner.PSourcePlaceholderSync
> output

[output] UI state after the event <output>
= The text changed event has completed through the current implementation.
! Code binding: cs|PPanels/PSource.cs|PSource|pSourcePathBox|TextChanged|1
