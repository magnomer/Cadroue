@format 1
@id ui-event.7e7d9bbdde6e177f
@title Prefix box — key down
@section I. UI
@area UI event
@tabs Split
@event-ref cs|PFlow/PFlowSectionName.cs|PFlowNameShow|pPrefixBox|KeyDown|1
@entry input
@summary Handle the key down event exposed by PFlow.PFlowNameShow and follow its registered code path.
@tag key down
@tag PFlow
@alias owner = Cadroue.UIShell.PFlow.PFlow

[input] UI event <input>
- Receive the key down event from the prefix box @ owner.PFlowNameShow
> route

[route] Registered handler path <process>
- Handle flow name @ owner.PFlowNameHandle
- Apply flow name @ owner.PFlowNameApply
- Close flow name @ owner.PFlowNameClose
- Evaluate the handler's implemented conditional branches @ owner.PFlowNameHandle
- Mark the routed event handled on the implemented paths @ owner.PFlowNameHandle
> output

[output] UI state after the event <output>
= The key down event has completed through the current implementation.
! Code binding: cs|PFlow/PFlowSectionName.cs|PFlowNameShow|pPrefixBox|KeyDown|1
