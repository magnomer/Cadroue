@format 1
@id ui-event.5cdcc8f0389bcb73
@title Field box — preview text input
@section I. UI
@area UI event
@tabs Split
@event-ref cs|PFlow/PFlowSectionName.cs|PFlowStepAttach|pFieldBox|PreviewTextInput|1
@entry input
@summary Handle the preview text input event exposed by PFlow.PFlowStepAttach and follow its registered code path.
@tag preview text input
@tag PFlow
@alias owner = Cadroue.UIShell.PFlow.PFlow

[input] UI event <input>
- Receive the preview text input event from the field box @ owner.PFlowStepAttach
> route

[route] Registered handler path <process>
- Show flow affix @ owner.PFlowAffixShow
- Evaluate the handler's implemented conditional branches @ owner.PFlowAffixShow
- Mark the routed event handled on the implemented paths @ owner.PFlowAffixShow
- Update the visual or selection state owned by the handler @ owner.PFlowAffixShow
> output

[output] UI state after the event <output>
= The preview text input event has completed through the current implementation.
! Code binding: cs|PFlow/PFlowSectionName.cs|PFlowStepAttach|pFieldBox|PreviewTextInput|1
