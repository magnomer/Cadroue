@format 1
@id split.section-add
@title Add section
@area UI workflow
@entry input
@summary Create a new section at the current cursor and synchronize every section representation.
@section I. UI
@tabs Split
@flow TB
@related shared.synchronize-sections
@tag section
@tag section-add

@alias flow = Cadroue.UIShell.PFlow.PFlow
@alias segment = Cadroue.Core.LSegment

[input] Add section command <input>
- Receive the section command in the active Split timeline @ flow.PFlowSectionAdd(...)
> validate

[validate] Validate section request <decision>
- Refuse section creation when the active source is locked by accepted work @ flow.PFlowSectionAdd(...)
- Validate the current cursor, selection, duration, and overlap constraints @ segment.LSegmentAdd(...)
? "The source is editable and a valid plan is produced" > apply
? "The source is locked or the request is invalid" > unchanged

[apply] Apply section plan <process>
- Replace the section list and active selection with the returned plan @ flow.PFlowSectionApply(...)
- Record the new section in the log @ flow.PFlowSectionRecord(...)
> fragment:shared.synchronize-sections

[unchanged] No change <output>
=The request does not change the section model.
