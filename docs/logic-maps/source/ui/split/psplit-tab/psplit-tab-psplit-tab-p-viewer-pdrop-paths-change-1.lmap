@format 1
@id ui-event.9e0a09d9cca8df7e
@title Viewer — drop paths change
@section I. UI
@area UI event
@tabs Split
@event-ref cs|PMainArea/PSplitTab.cs|PSplitTab|pViewer|PDropPathsChange|1
@entry input
@summary Handle the drop paths change event emitted by Viewer through the exact route registered in PSplitTab.PSplitTab.
@tag drop paths change
@tag PSplitTab
@alias owner = Cadroue.UIShell.PMainArea.PSplitTab

[input] UI event <input>
- Receive the drop paths change event from Viewer through its current registration @ owner.PSplitTab
> route

[route] Registered handler path <process>
- Execute the registered delegate without substituting a different route @ owner.PSplitTab
! Registered call path: pList.PListPathsAdd
> output

[output] UI state after the event <output>
= The drop paths change event has completed through the current implementation.
! Code binding: cs|PMainArea/PSplitTab.cs|PSplitTab|pViewer|PDropPathsChange|1
