@format 1
@id ui-event.e5ecf7475848205b
@title Action — all add
@section I. UI
@area UI event
@tabs Split
@event-ref cs|PMainArea/PSplitTab.cs|PSplitTab|pAction|PActionAllAdd|1
@entry input
@summary Handle the all add event emitted by Action through the exact route registered in PSplitTab.PSplitTab.
@tag all add
@tag PSplitTab
@alias owner = Cadroue.UIShell.PMainArea.PSplitTab
@alias list = Cadroue.UIShell.PPanels.PList

[input] UI event <input>
- Receive the all add event from Action through its current registration @ owner.PSplitTab
> route

[route] Registered handler path <process>
- Select only unlocked list items and preserve each relay batch in the work-source request @ list.PListUnlockedRead
- Submit those sources through the registered delegate @ owner.PSplitTab
! Registered call path: LSplit.LSplitAllDescribe → pList.PListUnlockedRead → Select → LWorkSource → ToArray
> output

[output] UI state after the event <output>
= The all add event has completed through the current implementation.
! Code binding: cs|PMainArea/PSplitTab.cs|PSplitTab|pAction|PActionAllAdd|1
