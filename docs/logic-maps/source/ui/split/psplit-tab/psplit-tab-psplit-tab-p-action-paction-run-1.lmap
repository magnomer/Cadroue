@format 1
@id ui-event.db6ba2ce547b691c
@title Action — run
@section I. UI
@area UI event
@tabs Split
@event-ref cs|PMainArea/PSplitTab.cs|PSplitTab|pAction|PActionRun|1
@entry input
@summary Handle the run event emitted by Action through the exact route registered in PSplitTab.PSplitTab.
@tag run
@tag PSplitTab
@alias owner = Cadroue.UIShell.PMainArea.PSplitTab
@alias list = Cadroue.UIShell.PPanels.PList

[input] UI event <input>
- Receive the run event from Action through its current registration @ owner.PSplitTab
> route

[route] Registered handler path <process>
- Require the current list item to be unlocked before submitting it @ list.PListUnlockedItemRead
- Submit its path, sections, and relay batch through the registered delegate @ owner.PSplitTab
! Registered call path: pList.PListUnlockedItemRead → LSplit.LSplitDescribe → pSection.PSectionSplitRead
> output

[output] UI state after the event <output>
= The run event has completed through the current implementation.
! Code binding: cs|PMainArea/PSplitTab.cs|PSplitTab|pAction|PActionRun|1
