@format 1
@id ui-event.90efc4a16d86904e
@title List — path change
@section I. UI
@area UI event
@tabs Split
@event-ref cs|PMainArea/PSplitTab.cs|PSplitTab|pList|PListPathChange|1
@entry input
@summary Handle the path change event emitted by List through the exact route registered in PSplitTab.PSplitTab.
@tag path change
@tag PSplitTab
@alias owner = Cadroue.UIShell.PMainArea.PSplitTab

[input] UI event <input>
- Receive the path change event from List through its current registration @ owner.PSplitTab
> route

[route] Registered handler path <process>
- Invoke the registered owner handler @ owner.PSplitPathShow
! Registered call path: PSplitPathShow
> output

[output] UI state after the event <output>
= The path change event has completed through the current implementation.
! Code binding: cs|PMainArea/PSplitTab.cs|PSplitTab|pList|PListPathChange|1
