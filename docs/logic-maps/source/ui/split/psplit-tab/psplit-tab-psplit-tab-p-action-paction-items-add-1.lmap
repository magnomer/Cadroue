@format 1
@id ui-event.ea9ab019fc364e55
@title Action — items add
@section I. UI
@area UI event
@tabs Split
@event-ref cs|PMainArea/PSplitTab.cs|PSplitTab|pAction|PActionItemsAdd|1
@entry input
@summary Handle the items add event emitted by Action through the exact route registered in PSplitTab.PSplitTab.
@tag items add
@tag PSplitTab
@alias owner = Cadroue.UIShell.PMainArea.PSplitTab
@alias list = Cadroue.UIShell.PPanels.PList

[input] UI event <input>
- Receive the items add event from Action through its current registration @ owner.PSplitTab
> route

[route] Registered handler path <process>
- Intersect the requested paths with unlocked list items and preserve each relay batch @ list.PListUnlockedItemsRead
- Submit those sources through the registered delegate @ owner.PSplitTab
! Registered call path: LSplit.LSplitAllDescribe → pList.PListUnlockedItemsRead → Where → pSplitPaths.Contains → Select → LWorkSource → ToArray
> output

[output] UI state after the event <output>
= The items add event has completed through the current implementation.
! Code binding: cs|PMainArea/PSplitTab.cs|PSplitTab|pAction|PActionItemsAdd|1
