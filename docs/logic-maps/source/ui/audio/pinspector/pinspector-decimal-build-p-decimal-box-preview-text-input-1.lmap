@format 1
@id ui-event.8b26000f00d92f80
@title Decimal box — preview text input
@section I. UI
@area UI event
@tabs Audio
@event-ref cs|PPanels/PInspectorVolume.cs|PInspectorDecimalBuild|pDecimalBox|PreviewTextInput|1
@entry input
@summary Handle the preview text input event exposed by PInspector.PInspectorDecimalBuild and follow its registered code path.
@tag preview text input
@tag PInspector
@alias owner = Cadroue.UIShell.PPanels.PInspector

[input] UI event <input>
- Receive the preview text input event from the decimal box @ owner.PInspectorDecimalBuild
> route

[route] Registered handler path <process>
- Mark the routed event handled on the implemented paths @ owner.PInspectorDecimalBuild
> output

[output] UI state after the event <output>
= The preview text input event has completed through the current implementation.
! Code binding: cs|PPanels/PInspectorVolume.cs|PInspectorDecimalBuild|pDecimalBox|PreviewTextInput|1
