@format 1
@id ui-event.3a3476409ee3bcba
@title Poles — selection changed
@section I. UI
@area UI event
@tabs Audio
@event-ref cs|PPanels/PInspectorPass.cs|PInspectorPassBuild|pPoles|SelectionChanged|2
@entry input
@summary Handle the selection changed event exposed by PInspector.PInspectorPassBuild and follow its registered code path.
@tag selection changed
@tag PInspector
@alias owner = Cadroue.UIShell.PPanels.PInspector

[input] UI event <input>
- Receive the selection changed event from the poles @ owner.PInspectorPassBuild
> route

[route] Registered handler path <process>
- Execute p poles reflect @ owner.pPolesReflect
- Read filter poles @ owner.PFilterPolesRead
- Update the visual or selection state owned by the handler @ owner.pPolesReflect
> output

[output] UI state after the event <output>
= The selection changed event has completed through the current implementation.
! Code binding: cs|PPanels/PInspectorPass.cs|PInspectorPassBuild|pPoles|SelectionChanged|2
