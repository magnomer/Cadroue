@format 1
@id ui-event.fad64b6ca5fce001
@title Frequency box — text changed
@section I. UI
@area UI event
@tabs Audio
@event-ref cs|PPanels/PInspectorEqualizer.cs|PEqualizerRowAdd|pFrequencyBox|TextChanged|1
@entry input
@summary Handle the text changed event exposed by PInspector.PEqualizerRowAdd and follow its registered code path.
@tag text changed
@tag PInspector
@alias owner = Cadroue.UIShell.PPanels.PInspector

[input] UI event <input>
- Receive the text changed event from the frequency box @ owner.PEqualizerRowAdd
> route

[route] Registered handler path <process>
- Raise inspector active @ owner.PInspectorActiveRaise
> output

[output] UI state after the event <output>
= The text changed event has completed through the current implementation.
! Code binding: cs|PPanels/PInspectorEqualizer.cs|PEqualizerRowAdd|pFrequencyBox|TextChanged|1
