@format 1
@id ui-event.08b08d2c6024266d
@title Inspector volume slider — value changed
@section I. UI
@area UI event
@tabs Audio
@event-ref cs|PPanels/PInspectorVolume.cs|PVolumeBodyBuild|pInspectorVolumeSlider|ValueChanged|1
@entry input
@summary Handle the value changed event exposed by PInspector.PVolumeBodyBuild and follow its registered code path.
@tag value changed
@tag PInspector
@alias owner = Cadroue.UIShell.PPanels.PInspector

[input] UI event <input>
- Receive the value changed event from the inspector volume slider @ owner.PVolumeBodyBuild
> route

[route] Registered handler path <process>
- Update volume warn @ owner.PVolumeWarnUpdate
- Read inspector decimal @ owner.PInspectorDecimalRead
- Update the visual or selection state owned by the handler @ owner.PVolumeWarnUpdate
> output

[output] UI state after the event <output>
= The value changed event has completed through the current implementation.
! Code binding: cs|PPanels/PInspectorVolume.cs|PVolumeBodyBuild|pInspectorVolumeSlider|ValueChanged|1
