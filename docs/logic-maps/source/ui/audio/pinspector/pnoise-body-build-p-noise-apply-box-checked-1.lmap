@format 1
@id ui-event.0491299e10c3f239
@title Noise apply box — checked
@section I. UI
@area UI event
@tabs Audio
@event-ref cs|PPanels/PInspectorNoise.cs|PNoiseBodyBuild|pNoiseApplyBox|Checked|1
@entry input
@summary Handle the checked event exposed by PInspector.PNoiseBodyBuild and follow its registered code path.
@tag checked
@tag PInspector
@alias owner = Cadroue.UIShell.PPanels.PInspector

[input] UI event <input>
- Receive the checked event from the noise apply box @ owner.PNoiseBodyBuild
> route

[route] Registered handler path <process>
- Update noise apply @ owner.PNoiseApplyUpdate
- Raise inspector active @ owner.PInspectorActiveRaise
- Update the visual or selection state owned by the handler @ owner.PNoiseApplyUpdate
> output

[output] UI state after the event <output>
= The checked event has completed through the current implementation.
! Code binding: cs|PPanels/PInspectorNoise.cs|PNoiseBodyBuild|pNoiseApplyBox|Checked|1
