@format 1
@id ui-event.8f6ef5c162499713
@title Equalizer preset — selection changed
@section I. UI
@area UI event
@tabs Audio
@event-ref cs|PPanels/PInspectorEqualizer.cs|PEqualizerBodyBuild|pEqualizerPreset|SelectionChanged|1
@entry input
@summary Handle the selection changed event exposed by PInspector.PEqualizerBodyBuild and follow its registered code path.
@tag selection changed
@tag PInspector
@alias owner = Cadroue.UIShell.PPanels.PInspector

[input] UI event <input>
- Receive the selection changed event from the equalizer preset @ owner.PEqualizerBodyBuild
> route

[route] Registered handler path <process>
- Apply equalizer preset @ owner.PEqualizerPresetApply
- Read equalizer values @ owner.PEqualizerValuesRead
- Apply equalizer rows @ owner.PEqualizerRowsApply
- Reset equalizer custom @ owner.PEqualizerCustomReset
- Raise inspector active @ owner.PInspectorActiveRaise
- Evaluate the handler's implemented conditional branches @ owner.PEqualizerPresetApply
- Update the visual or selection state owned by the handler @ owner.PEqualizerPresetApply
> output

[output] UI state after the event <output>
= The selection changed event has completed through the current implementation.
! Code binding: cs|PPanels/PInspectorEqualizer.cs|PEqualizerBodyBuild|pEqualizerPreset|SelectionChanged|1
