@format 1
@id ui-event.a12a852e85b7b1f2
@title Inspector volume value — text changed
@section I. UI
@area UI event
@tabs Audio
@event-ref cs|PPanels/PInspectorVolume.cs|PVolumeBodyBuild|pInspectorVolumeValue|TextChanged|1
@entry input
@summary Handle the text changed event exposed by PInspector.PVolumeBodyBuild and follow its registered code path.
@tag text changed
@tag PInspector
@alias owner = Cadroue.UIShell.PPanels.PInspector

[input] UI event <input>
- Receive the text changed event from the inspector volume value @ owner.PVolumeBodyBuild
> route

[route] Registered handler path <process>
- Read inspector decimal @ owner.PInspectorDecimalRead
> output

[output] UI state after the event <output>
= The text changed event has completed through the current implementation.
! Code binding: cs|PPanels/PInspectorVolume.cs|PVolumeBodyBuild|pInspectorVolumeValue|TextChanged|1
