@format 1
@id ui-event.a31982ec61cb354c
@title Preset — selection changed
@section I. UI
@area UI event
@tabs Audio
@event-ref cs|PPanels/PInspectorPass.cs|PInspectorPassBuild|pPreset|SelectionChanged|1
@entry input
@summary Handle the selection changed event exposed by PInspector.PInspectorPassBuild and follow its registered code path.
@tag selection changed
@tag PInspector
@alias owner = Cadroue.UIShell.PPanels.PInspector

[input] UI event <input>
- Receive the selection changed event from the preset @ owner.PInspectorPassBuild
> route

[route] Registered handler path <process>
- Apply filter preset @ owner.PFilterPresetApply
- Read filter values @ owner.PFilterValuesRead
- Apply filter values @ owner.PFilterValuesApply
- Reset filter custom @ owner.PFilterCustomReset
- Raise inspector active @ owner.PInspectorActiveRaise
- Evaluate the handler's implemented conditional branches @ owner.PFilterPresetApply
- Update the visual or selection state owned by the handler @ owner.PFilterPresetApply
> output

[output] UI state after the event <output>
= The selection changed event has completed through the current implementation.
! Code binding: cs|PPanels/PInspectorPass.cs|PInspectorPassBuild|pPreset|SelectionChanged|1
