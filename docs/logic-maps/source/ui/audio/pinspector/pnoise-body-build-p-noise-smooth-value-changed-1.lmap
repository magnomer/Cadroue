@format 1
@id ui-event.a2629b385200aeb1
@title Noise smooth — value changed
@section I. UI
@area UI event
@tabs Audio
@event-ref cs|PPanels/PInspectorNoise.cs|PNoiseBodyBuild|pNoiseSmooth|ValueChanged|1
@entry input
@summary Handle the value changed event exposed by PInspector.PNoiseBodyBuild and follow its registered code path.
@tag value changed
@tag PInspector
@alias owner = Cadroue.UIShell.PPanels.PInspector

[input] UI event <input>
- Receive the value changed event from the noise smooth @ owner.PNoiseBodyBuild
> route

[route] Registered handler path <process>
- Check noise deviation @ owner.PNoiseDeviationCheck
- Execute noise values match @ owner.PNoiseValuesMatch
- Reset noise custom @ owner.PNoiseCustomReset
- Select noise preset @ owner.PNoisePresetSelect
- Set noise custom @ owner.PNoiseCustomSet
- Evaluate the handler's implemented conditional branches @ owner.PNoiseDeviationCheck
> output

[output] UI state after the event <output>
= The value changed event has completed through the current implementation.
! Code binding: cs|PPanels/PInspectorNoise.cs|PNoiseBodyBuild|pNoiseSmooth|ValueChanged|1
