@format 1
@id ui-event.b993d4ec6121118f
@title Apply — unchecked
@section I. UI
@area UI event
@tabs Audio
@event-ref cs|PPanels/PInspectorPass.cs|PInspectorPassBuild|pApply|Unchecked|1
@entry input
@summary Handle the unchecked event exposed by PInspector.PInspectorPassBuild and follow its registered code path.
@tag unchecked
@tag PInspector
@alias owner = Cadroue.UIShell.PPanels.PInspector

[input] UI event <input>
- Receive the unchecked event from the apply @ owner.PInspectorPassBuild
> route

[route] Registered handler path <process>
- Update filter apply @ owner.PFilterApplyUpdate
- Raise inspector active @ owner.PInspectorActiveRaise
- Update the visual or selection state owned by the handler @ owner.PFilterApplyUpdate
> output

[output] UI state after the event <output>
= The unchecked event has completed through the current implementation.
! Code binding: cs|PPanels/PInspectorPass.cs|PInspectorPassBuild|pApply|Unchecked|1
