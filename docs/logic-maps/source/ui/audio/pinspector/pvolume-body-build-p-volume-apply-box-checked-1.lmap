@format 1
@id ui-event.6ad102bb9f13d04b
@title Volume apply box — checked
@section I. UI
@area UI event
@tabs Audio
@event-ref cs|PPanels/PInspectorVolume.cs|PVolumeBodyBuild|pVolumeApplyBox|Checked|1
@entry input
@summary Handle the checked event exposed by PInspector.PVolumeBodyBuild and follow its registered code path.
@tag checked
@tag PInspector
@alias owner = Cadroue.UIShell.PPanels.PInspector

[input] UI event <input>
- Receive the checked event from the volume apply box @ owner.PVolumeBodyBuild
> route

[route] Registered handler path <process>
- Update volume apply @ owner.PVolumeApplyUpdate
- Raise inspector active @ owner.PInspectorActiveRaise
- Update the visual or selection state owned by the handler @ owner.PVolumeApplyUpdate
> output

[output] UI state after the event <output>
= The checked event has completed through the current implementation.
! Code binding: cs|PPanels/PInspectorVolume.cs|PVolumeBodyBuild|pVolumeApplyBox|Checked|1
