@format 1
@id ui-event.b9b200e31a84aea7
@title Equalizer apply box — unchecked
@section I. UI
@area UI event
@tabs Audio
@event-ref cs|PPanels/PInspectorEqualizer.cs|PEqualizerBodyBuild|pEqualizerApplyBox|Unchecked|1
@entry input
@summary Handle the unchecked event exposed by PInspector.PEqualizerBodyBuild and follow its registered code path.
@tag unchecked
@tag PInspector
@alias owner = Cadroue.UIShell.PPanels.PInspector

[input] UI event <input>
- Receive the unchecked event from the equalizer apply box @ owner.PEqualizerBodyBuild
> route

[route] Registered handler path <process>
- Update equalizer apply @ owner.PEqualizerApplyUpdate
- Raise inspector active @ owner.PInspectorActiveRaise
- Update the visual or selection state owned by the handler @ owner.PEqualizerApplyUpdate
> output

[output] UI state after the event <output>
= The unchecked event has completed through the current implementation.
! Code binding: cs|PPanels/PInspectorEqualizer.cs|PEqualizerBodyBuild|pEqualizerApplyBox|Unchecked|1
