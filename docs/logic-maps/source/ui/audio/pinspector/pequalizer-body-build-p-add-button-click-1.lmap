@format 1
@id ui-event.b35421d1ce6088e9
@title Add button — click
@section I. UI
@area UI event
@tabs Audio
@event-ref cs|PPanels/PInspectorEqualizer.cs|PEqualizerBodyBuild|pAddButton|Click|1
@entry input
@summary Handle the click event exposed by PInspector.PEqualizerBodyBuild and follow its registered code path.
@tag click
@tag PInspector
@alias owner = Cadroue.UIShell.PPanels.PInspector

[input] UI event <input>
- Receive the click event from the add button @ owner.PEqualizerBodyBuild
> route

[route] Registered handler path <process>
- Add equalizer row @ owner.PEqualizerRowAdd
- Build inspector decimal @ owner.PInspectorDecimalBuild
- Execute static @ owner.static
- Build equalizer unit @ owner.PEqualizerUnitBuild
- Raise inspector active @ owner.PInspectorActiveRaise
- Check equalizer deviation @ owner.PEqualizerDeviationCheck
- Evaluate the handler's implemented conditional branches @ owner.PEqualizerRowAdd
- Update the visual or selection state owned by the handler @ owner.PEqualizerRowAdd
> output

[output] UI state after the event <output>
= The click event has completed through the current implementation.
! Code binding: cs|PPanels/PInspectorEqualizer.cs|PEqualizerBodyBuild|pAddButton|Click|1
