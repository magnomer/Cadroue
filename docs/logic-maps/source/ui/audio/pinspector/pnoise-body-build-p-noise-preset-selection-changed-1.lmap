@format 1
@id ui-event.fcc2cf0ff4af1bad
@title Noise preset — selection changed
@section I. UI
@area UI event
@tabs Audio
@event-ref cs|PPanels/PInspectorNoise.cs|PNoiseBodyBuild|pNoisePreset|SelectionChanged|1
@entry input
@summary Handle the selection changed event exposed by PInspector.PNoiseBodyBuild and follow its registered code path.
@tag selection changed
@tag PInspector
@alias owner = Cadroue.UIShell.PPanels.PInspector

[input] UI event <input>
- Receive the selection changed event from the noise preset @ owner.PNoiseBodyBuild
> route

[route] Registered handler path <process>
- Apply noise preset @ owner.PNoisePresetApply
- Apply noise values @ owner.PNoiseValuesApply
- Reset noise custom @ owner.PNoiseCustomReset
- Raise inspector active @ owner.PInspectorActiveRaise
- Evaluate the handler's implemented conditional branches @ owner.PNoisePresetApply
- Update the visual or selection state owned by the handler @ owner.PNoisePresetApply
> output

[output] UI state after the event <output>
= The selection changed event has completed through the current implementation.
! Code binding: cs|PPanels/PInspectorNoise.cs|PNoiseBodyBuild|pNoisePreset|SelectionChanged|1
