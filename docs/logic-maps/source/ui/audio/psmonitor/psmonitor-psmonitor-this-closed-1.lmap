@format 1
@id ui-event.8b5c0278af3663c9
@title S Monitor — closed
@section I. UI
@area UI event
@tabs Audio
@event-ref cs|PPanels/PSMonitor.cs|PSMonitor|this|Closed|1
@entry input
@summary Handle the closed event emitted by S Monitor through the exact route registered in PSMonitor.PSMonitor.
@tag closed
@tag PSMonitor
@alias owner = Cadroue.UIShell.PPanels.PSMonitor

[input] UI event <input>
- Receive the closed event from S Monitor through its current registration @ owner.PSMonitor
> route

[route] Registered handler path <process>
- Invoke the registered owner handler @ owner.PSMonitorCloseHandle
! Registered call path: PSMonitorCloseHandle
> output

[output] UI state after the event <output>
= The closed event has completed through the current implementation.
! Code binding: cs|PPanels/PSMonitor.cs|PSMonitor|this|Closed|1
