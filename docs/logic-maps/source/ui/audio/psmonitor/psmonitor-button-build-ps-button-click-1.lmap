@format 1
@id ui-event.5c37b21e3906264c
@title Button — click
@section I. UI
@area UI event
@tabs Audio
@event-ref cs|PPanels/PSMonitor.cs|PSMonitorButtonBuild|psButton|Click|1
@entry input
@summary Handle the click event exposed by PSMonitor.PSMonitorButtonBuild and follow its registered code path.
@tag click
@tag PSMonitor
@alias owner = Cadroue.UIShell.PPanels.PSMonitor

[input] UI event <input>
- Receive the click event from the button @ owner.PSMonitorButtonBuild
> route

[route] Registered handler path <process>
- Continue through the implementation owned by the event handler @ owner.PSMonitorButtonBuild
> output

[output] UI state after the event <output>
= The click event has completed through the current implementation.
! Code binding: cs|PPanels/PSMonitor.cs|PSMonitorButtonBuild|psButton|Click|1
