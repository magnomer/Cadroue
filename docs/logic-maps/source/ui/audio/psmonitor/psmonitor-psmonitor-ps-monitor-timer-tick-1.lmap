@format 1
@id ui-event.884b0755779adf69
@title ps Monitor Timer — tick
@section I. UI
@area UI event
@tabs Audio
@event-ref cs|PPanels/PSMonitor.cs|PSMonitor|psMonitorTimer|Tick|1
@entry input
@summary Handle the tick event emitted by ps Monitor Timer through the exact route registered in PSMonitor.PSMonitor.
@tag tick
@tag PSMonitor
@alias owner = Cadroue.UIShell.PPanels.PSMonitor

[input] UI event <input>
- Receive the tick event from ps Monitor Timer through its current registration @ owner.PSMonitor
> route

[route] Registered handler path <process>
- Invoke the registered owner handler @ owner.PSMonitorTickHandle
! Registered call path: PSMonitorTickHandle
> output

[output] UI state after the event <output>
= The tick event has completed through the current implementation.
! Code binding: cs|PPanels/PSMonitor.cs|PSMonitor|psMonitorTimer|Tick|1
