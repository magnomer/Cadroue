@format 1
@id ui-event.9cf9ab291c7bb6ce
@title Row border — mouse left button down
@section I. UI
@area UI event
@tabs Merge
@event-ref cs|PPanels/PGroup.cs|PGroupFileBuild|pRowBorder|MouseLeftButtonDown|1
@entry input
@summary Handle the mouse left button down event exposed by PGroup.PGroupFileBuild and follow its registered code path.
@tag mouse left button down
@tag PGroup
@alias owner = Cadroue.UIShell.PPanels.PGroup

[input] UI event <input>
- Receive the mouse left button down event from the row border @ owner.PGroupFileBuild
> route

[route] Registered handler path <process>
- Update the visual or selection state owned by the handler @ owner.PGroupFileBuild
> output

[output] UI state after the event <output>
= The mouse left button down event has completed through the current implementation.
! Code binding: cs|PPanels/PGroup.cs|PGroupFileBuild|pRowBorder|MouseLeftButtonDown|1
