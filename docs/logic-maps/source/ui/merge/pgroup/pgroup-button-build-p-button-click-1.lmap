@format 1
@id ui-event.22eb8458b5d748fb
@title Button — click
@section I. UI
@area UI event
@tabs Merge
@event-ref cs|PPanels/PGroup.cs|PGroupButtonBuild|pButton|Click|1
@entry input
@summary Handle the click event exposed by PGroup.PGroupButtonBuild and follow its registered code path.
@tag click
@tag PGroup
@alias owner = Cadroue.UIShell.PPanels.PGroup

[input] UI event <input>
- Receive the click event from the button @ owner.PGroupButtonBuild
> route

[route] Registered handler path <process>
- Continue through the implementation owned by the event handler @ owner.PGroupButtonBuild
> output

[output] UI state after the event <output>
= The click event has completed through the current implementation.
! Code binding: cs|PPanels/PGroup.cs|PGroupButtonBuild|pButton|Click|1
