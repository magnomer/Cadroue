@format 1
@id ui-event.c752997ef4453395
@title Row border — mouse move
@section I. UI
@area UI event
@tabs Merge
@event-ref cs|PPanels/PGroup.cs|PGroupFileBuild|pRowBorder|MouseMove|1
@entry input
@summary Handle the mouse move event exposed by PGroup.PGroupFileBuild and follow its registered code path.
@tag mouse move
@tag PGroup
@alias owner = Cadroue.UIShell.PPanels.PGroup

[input] UI event <input>
- Receive the mouse move event from the row border @ owner.PGroupFileBuild
> route

[route] Registered handler path <process>
- Handle group drag @ owner.PGroupDragHandle
- Clear group drag @ owner.PGroupDragClear
- Evaluate the handler's implemented conditional branches @ owner.PGroupDragHandle
> output

[output] UI state after the event <output>
= The mouse move event has completed through the current implementation.
! Code binding: cs|PPanels/PGroup.cs|PGroupFileBuild|pRowBorder|MouseMove|1
