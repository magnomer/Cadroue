@format 1
@id ui-event.7eed05730101ed65
@title Name box — key down
@section I. UI
@area UI event
@tabs Merge
@event-ref cs|PPanels/PGroup.cs|PGroupEditStart|pNameBox|KeyDown|1
@entry input
@summary Handle the key down event exposed by PGroup.PGroupEditStart and follow its registered code path.
@tag key down
@tag PGroup
@alias owner = Cadroue.UIShell.PPanels.PGroup

[input] UI event <input>
- Receive the key down event from the name box @ owner.PGroupEditStart
> route

[route] Registered handler path <process>
- Commit group name @ owner.PGroupNameCommit
- Rebuild group @ owner.PGroupRebuild
- Evaluate the handler's implemented conditional branches @ owner.PGroupNameCommit
- Mark the routed event handled on the implemented paths @ owner.PGroupNameCommit
> output

[output] UI state after the event <output>
= The key down event has completed through the current implementation.
! Code binding: cs|PPanels/PGroup.cs|PGroupEditStart|pNameBox|KeyDown|1
