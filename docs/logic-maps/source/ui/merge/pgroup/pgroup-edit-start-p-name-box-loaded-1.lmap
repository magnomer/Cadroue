@format 1
@id ui-event.9dd859f7c4874b33
@title Name box — loaded
@section I. UI
@area UI event
@tabs Merge
@event-ref cs|PPanels/PGroup.cs|PGroupEditStart|pNameBox|Loaded|1
@entry input
@summary Handle the loaded event exposed by PGroup.PGroupEditStart and follow its registered code path.
@tag loaded
@tag PGroup
@alias owner = Cadroue.UIShell.PPanels.PGroup

[input] UI event <input>
- Receive the loaded event from the name box @ owner.PGroupEditStart
> route

[route] Registered handler path <process>
- Evaluate the handler's implemented conditional branches @ owner.PGroupEditStart
- Mark the routed event handled on the implemented paths @ owner.PGroupEditStart
- Update the visual or selection state owned by the handler @ owner.PGroupEditStart
> output

[output] UI state after the event <output>
= The loaded event has completed through the current implementation.
! Code binding: cs|PPanels/PGroup.cs|PGroupEditStart|pNameBox|Loaded|1
