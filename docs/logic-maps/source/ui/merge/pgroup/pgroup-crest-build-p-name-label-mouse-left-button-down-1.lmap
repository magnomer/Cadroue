@format 1
@id ui-event.f16c834669f3954f
@title Name label — mouse left button down
@section I. UI
@area UI event
@tabs Merge
@event-ref cs|PPanels/PGroup.cs|PGroupCrestBuild|pNameLabel|MouseLeftButtonDown|1
@entry input
@summary Handle the mouse left button down event exposed by PGroup.PGroupCrestBuild and follow its registered code path.
@tag mouse left button down
@tag PGroup
@alias owner = Cadroue.UIShell.PPanels.PGroup

[input] UI event <input>
- Receive the mouse left button down event from the name label @ owner.PGroupCrestBuild
> route

[route] Registered handler path <process>
- Start group edit @ owner.PGroupEditStart
- Commit group name @ owner.PGroupNameCommit
- Rebuild group @ owner.PGroupRebuild
- Evaluate the handler's implemented conditional branches @ owner.PGroupEditStart
- Mark the routed event handled on the implemented paths @ owner.PGroupEditStart
- Update the visual or selection state owned by the handler @ owner.PGroupEditStart
> output

[output] UI state after the event <output>
= The mouse left button down event has completed through the current implementation.
! Code binding: cs|PPanels/PGroup.cs|PGroupCrestBuild|pNameLabel|MouseLeftButtonDown|1
