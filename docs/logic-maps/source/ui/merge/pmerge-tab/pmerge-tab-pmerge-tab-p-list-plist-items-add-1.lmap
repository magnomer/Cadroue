@format 1
@id ui-event.5fad331e8e80dae3
@title List — items add
@section I. UI
@area UI event
@tabs Merge
@event-ref cs|PMainArea/PMergeTab.cs|PMergeTab|pList|PListItemsAdd|1
@entry input
@summary Handle the items add event emitted by List through the exact route registered in PMergeTab.PMergeTab.
@tag items add
@tag PMergeTab
@alias owner = Cadroue.UIShell.PMainArea.PMergeTab

[input] UI event <input>
- Receive the items add event from List through its current registration @ owner.PMergeTab
> route

[route] Registered handler path <process>
- Invoke the registered owner handler @ owner.PMergeItemsHandle
! Registered call path: PMergeItemsHandle
> output

[output] UI state after the event <output>
= The items add event has completed through the current implementation.
! Code binding: cs|PMainArea/PMergeTab.cs|PMergeTab|pList|PListItemsAdd|1
