@format 1
@id ui-event.a83906d328406538
@title Action — run
@section I. UI
@area UI event
@tabs Merge
@event-ref cs|PMainArea/PMergeTab.cs|PMergeTab|pAction|PActionRun|1
@entry input
@summary Handle the run event emitted by Action through the exact route registered in PMergeTab.PMergeTab.
@tag run
@tag PMergeTab
@alias owner = Cadroue.UIShell.PMainArea.PMergeTab

[input] UI event <input>
- Receive the run event from Action through its current registration @ owner.PMergeTab
> route

[route] Registered handler path <process>
- Invoke the registered owner handler @ owner.PMergeGroupsRead
- Continue through the owner callback @ owner.PMergeRelaysRead
! Registered call path: LMerge.LMergeDescribe → PMergeGroupsRead → PMergeRelaysRead
> output

[output] UI state after the event <output>
= The run event has completed through the current implementation.
! Code binding: cs|PMainArea/PMergeTab.cs|PMergeTab|pAction|PActionRun|1
