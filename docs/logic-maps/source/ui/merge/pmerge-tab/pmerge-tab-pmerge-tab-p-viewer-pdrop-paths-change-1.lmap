@format 1
@id ui-event.daff4e217e09ea84
@title Viewer — drop paths change
@section I. UI
@area UI event
@tabs Merge
@event-ref cs|PMainArea/PMergeTab.cs|PMergeTab|pViewer|PDropPathsChange|1
@entry input
@summary Handle the drop paths change event emitted by Viewer through the exact route registered in PMergeTab.PMergeTab.
@tag drop paths change
@tag PMergeTab
@alias owner = Cadroue.UIShell.PMainArea.PMergeTab

[input] UI event <input>
- Receive the drop paths change event from Viewer through its current registration @ owner.PMergeTab
> route

[route] Registered handler path <process>
- Execute the registered delegate without substituting a different route @ owner.PMergeTab
! Registered call path: pList.PListPathsAdd
> output

[output] UI state after the event <output>
= The drop paths change event has completed through the current implementation.
! Code binding: cs|PMainArea/PMergeTab.cs|PMergeTab|pViewer|PDropPathsChange|1
