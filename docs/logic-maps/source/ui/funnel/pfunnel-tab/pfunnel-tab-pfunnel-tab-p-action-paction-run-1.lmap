@format 1
@id ui-event.191d72177810dd7e
@title Action — run
@section I. UI
@area UI event
@tabs Funnel
@event-ref cs|PMainArea/PFunnelTab.cs|PFunnelTab|pAction|PActionRun|1
@entry input
@summary Handle the run event emitted by Action through the exact route registered in PFunnelTab.PFunnelTab.
@tag run
@tag PFunnelTab
@alias owner = Cadroue.UIShell.PMainArea.PFunnelTab

[input] UI event <input>
- Receive the run event from Action through its current registration @ owner.PFunnelTab
> route

[route] Registered handler path <process>
- Invoke the registered owner handler @ owner.PFunnelDispatch
! Registered call path: PFunnelDispatch → pList.PListItemRead
> output

[output] UI state after the event <output>
= The run event has completed through the current implementation.
! Code binding: cs|PMainArea/PFunnelTab.cs|PFunnelTab|pAction|PActionRun|1
