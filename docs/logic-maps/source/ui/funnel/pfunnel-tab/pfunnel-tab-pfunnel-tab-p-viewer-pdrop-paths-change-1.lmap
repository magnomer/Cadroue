@format 1
@id ui-event.6eefb59bc83c5da6
@title Viewer — drop paths change
@section I. UI
@area UI event
@tabs Funnel
@event-ref cs|PMainArea/PFunnelTab.cs|PFunnelTab|pViewer|PDropPathsChange|1
@entry input
@summary Handle the drop paths change event emitted by Viewer through the exact route registered in PFunnelTab.PFunnelTab.
@tag drop paths change
@tag PFunnelTab
@alias owner = Cadroue.UIShell.PMainArea.PFunnelTab

[input] UI event <input>
- Receive the drop paths change event from Viewer through its current registration @ owner.PFunnelTab
> route

[route] Registered handler path <process>
- Execute the registered delegate without substituting a different route @ owner.PFunnelTab
! Registered call path: pList.PListPathsAdd
> output

[output] UI state after the event <output>
= The drop paths change event has completed through the current implementation.
! Code binding: cs|PMainArea/PFunnelTab.cs|PFunnelTab|pViewer|PDropPathsChange|1
