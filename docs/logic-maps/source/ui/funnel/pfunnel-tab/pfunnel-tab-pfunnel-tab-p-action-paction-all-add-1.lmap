@format 1
@id ui-event.7ab21e3ba399d00e
@title Action — all add
@section I. UI
@area UI event
@tabs Funnel
@event-ref cs|PMainArea/PFunnelTab.cs|PFunnelTab|pAction|PActionAllAdd|1
@entry input
@summary Handle the all add event emitted by Action through the exact route registered in PFunnelTab.PFunnelTab.
@tag all add
@tag PFunnelTab
@alias owner = Cadroue.UIShell.PMainArea.PFunnelTab

[input] UI event <input>
- Receive the all add event from Action through its current registration @ owner.PFunnelTab
> route

[route] Registered handler path <process>
- Invoke the registered owner handler @ owner.PFunnelDispatch
! Registered call path: PFunnelDispatch → pList.PListItemsRead
> output

[output] UI state after the event <output>
= The all add event has completed through the current implementation.
! Code binding: cs|PMainArea/PFunnelTab.cs|PFunnelTab|pAction|PActionAllAdd|1
