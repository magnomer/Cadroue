@format 1
@id ui-event.3c69c0bb65be70e6
@title Button — click
@section I. UI
@area UI event
@tabs Funnel
@event-ref cs|PPanels/PFunnelRuleFrame.cs|PFunnelRemoveBuild|pButton|Click|1
@entry input
@summary Handle the click event exposed by PFunnelRuleFrame.PFunnelRemoveBuild and follow its registered code path.
@tag click
@tag PFunnelRuleFrame
@alias owner = Cadroue.UIShell.PPanels.PFunnelRuleFrame

[input] UI event <input>
- Receive the click event from the button @ owner.PFunnelRemoveBuild
> route

[route] Registered handler path <process>
- Continue through the implementation owned by the event handler @ owner.PFunnelRemoveBuild
> output

[output] UI state after the event <output>
= The click event has completed through the current implementation.
! Code binding: cs|PPanels/PFunnelRuleFrame.cs|PFunnelRemoveBuild|pButton|Click|1
