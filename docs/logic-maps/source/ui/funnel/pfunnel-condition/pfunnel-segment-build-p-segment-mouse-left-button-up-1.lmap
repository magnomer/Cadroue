@format 1
@id ui-event.4bd34a8ecb13e1f9
@title Segment — mouse left button up
@section I. UI
@area UI event
@tabs Funnel
@event-ref cs|PPanels/PFunnelCondition.cs|PFunnelSegmentBuild|pSegment|MouseLeftButtonUp|1
@entry input
@summary Handle the mouse left button up event exposed by PFunnelCondition.PFunnelSegmentBuild and follow its registered code path.
@tag mouse left button up
@tag PFunnelCondition
@alias owner = Cadroue.UIShell.PPanels.PFunnelCondition

[input] UI event <input>
- Receive the mouse left button up event from the segment @ owner.PFunnelSegmentBuild
> route

[route] Registered handler path <process>
- Continue through the implementation owned by the event handler @ owner.PFunnelSegmentBuild
> output

[output] UI state after the event <output>
= The mouse left button up event has completed through the current implementation.
! Code binding: cs|PPanels/PFunnelCondition.cs|PFunnelSegmentBuild|pSegment|MouseLeftButtonUp|1
