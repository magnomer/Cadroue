@format 1
@id ui-event.1827900a7cab9c33
@title Funnel whole check — unchecked
@section I. UI
@area UI event
@tabs Funnel
@event-ref cs|PPanels/PFunnelRuleRow.cs|PFunnelRegexBuild|pFunnelWholeCheck|Unchecked|1
@entry input
@summary Handle the unchecked event exposed by PFunnelRuleRow.PFunnelRegexBuild and follow its registered code path.
@tag unchecked
@tag PFunnelRuleRow
@alias owner = Cadroue.UIShell.PPanels.PFunnelRuleRow

[input] UI event <input>
- Receive the unchecked event from the funnel whole check @ owner.PFunnelRegexBuild
> route

[route] Registered handler path <process>
- Update the visual or selection state owned by the handler @ owner.PFunnelRegexBuild
> output

[output] UI state after the event <output>
= The unchecked event has completed through the current implementation.
! Code binding: cs|PPanels/PFunnelRuleRow.cs|PFunnelRegexBuild|pFunnelWholeCheck|Unchecked|1
