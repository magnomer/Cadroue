@format 1
@id ui-event.1deeb6e81ebc29ad
@title Combo — drop down opened
@section I. UI
@area UI event
@tabs Funnel
@event-ref cs|PPanels/PFunnelRuleRow.cs|PFunnelRelayBuild|pCombo|DropDownOpened|1
@entry input
@summary Handle the drop down opened event exposed by PFunnelRuleRow.PFunnelRelayBuild and follow its registered code path.
@tag drop down opened
@tag PFunnelRuleRow
@alias owner = Cadroue.UIShell.PPanels.PFunnelRuleRow

[input] UI event <input>
- Receive the drop down opened event from the combo @ owner.PFunnelRelayBuild
> route

[route] Registered handler path <process>
- Rebuild funnel relay @ owner.PFunnelRelayRebuild
- Evaluate the handler's implemented conditional branches @ owner.PFunnelRelayRebuild
- Update the visual or selection state owned by the handler @ owner.PFunnelRelayRebuild
> output

[output] UI state after the event <output>
= The drop down opened event has completed through the current implementation.
! Code binding: cs|PPanels/PFunnelRuleRow.cs|PFunnelRelayBuild|pCombo|DropDownOpened|1
