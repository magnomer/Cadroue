@format 1
@id ui-event.bcc3be0add819509
@title Row p funnel header — mouse move
@section I. UI
@area UI event
@tabs Funnel
@event-ref cs|PPanels/PFunnelRules.cs|PFunnelRuleAdd|pRow.PFunnelHeader|MouseMove|1
@entry input
@summary Handle the mouse move event exposed by PFunnelRules.PFunnelRuleAdd and follow its registered code path.
@tag mouse move
@tag PFunnelRules
@alias owner = Cadroue.UIShell.PPanels.PFunnelRules

[input] UI event <input>
- Receive the mouse move event from the row p funnel header @ owner.PFunnelRuleAdd
> route

[route] Registered handler path <process>
- Handle funnel move @ owner.PFunnelMoveHandle
- Move funnel row @ owner.PFunnelRowMove
- Resolve funnel index @ owner.PFunnelIndexResolve
- Evaluate the handler's implemented conditional branches @ owner.PFunnelMoveHandle
- Mark the routed event handled on the implemented paths @ owner.PFunnelMoveHandle
> output

[output] UI state after the event <output>
= The mouse move event has completed through the current implementation.
! Code binding: cs|PPanels/PFunnelRules.cs|PFunnelRuleAdd|pRow.PFunnelHeader|MouseMove|1
