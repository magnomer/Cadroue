@format 1
@id ui-event.ac3ca44aae563473
@title Add button — click
@section I. UI
@area UI event
@tabs Funnel
@event-ref cs|PPanels/PFunnelRules.cs|PFunnelActionBuild|pAddButton|Click|1
@entry input
@summary Handle the click event exposed by PFunnelRules.PFunnelActionBuild and follow its registered code path.
@tag click
@tag PFunnelRules
@alias owner = Cadroue.UIShell.PPanels.PFunnelRules

[input] UI event <input>
- Receive the click event from the add button @ owner.PFunnelActionBuild
> route

[route] Registered handler path <process>
- Show funnel menu @ owner.PFunnelMenuShow
- Add funnel rule @ owner.PFunnelRuleAdd
- Update the visual or selection state owned by the handler @ owner.PFunnelMenuShow
> output

[output] UI state after the event <output>
= The click event has completed through the current implementation.
! Code binding: cs|PPanels/PFunnelRules.cs|PFunnelActionBuild|pAddButton|Click|1
