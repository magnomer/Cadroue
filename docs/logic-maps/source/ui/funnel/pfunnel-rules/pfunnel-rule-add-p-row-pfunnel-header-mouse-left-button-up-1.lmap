@format 1
@id ui-event.b08012795676596b
@title Row p funnel header — mouse left button up
@section I. UI
@area UI event
@tabs Funnel
@event-ref cs|PPanels/PFunnelRules.cs|PFunnelRuleAdd|pRow.PFunnelHeader|MouseLeftButtonUp|1
@entry input
@summary Handle the mouse left button up event exposed by PFunnelRules.PFunnelRuleAdd and follow its registered code path.
@tag mouse left button up
@tag PFunnelRules
@alias owner = Cadroue.UIShell.PPanels.PFunnelRules

[input] UI event <input>
- Receive the mouse left button up event from the row p funnel header @ owner.PFunnelRuleAdd
> route

[route] Registered handler path <process>
- Handle funnel up @ owner.PFunnelUpHandle
- Select funnel row @ owner.PFunnelRowSelect
- Evaluate the handler's implemented conditional branches @ owner.PFunnelUpHandle
- Mark the routed event handled on the implemented paths @ owner.PFunnelUpHandle
> output

[output] UI state after the event <output>
= The mouse left button up event has completed through the current implementation.
! Code binding: cs|PPanels/PFunnelRules.cs|PFunnelRuleAdd|pRow.PFunnelHeader|MouseLeftButtonUp|1
