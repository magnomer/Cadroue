@format 1
@id ui-event.6a920ff61d30ced9
@title Remove button — click
@section I. UI
@area UI event
@tabs Funnel
@event-ref cs|PPanels/PFunnelRules.cs|PFunnelActionBuild|pRemoveButton|Click|1
@entry input
@summary Handle the click event exposed by PFunnelRules.PFunnelActionBuild and follow its registered code path.
@tag click
@tag PFunnelRules
@alias owner = Cadroue.UIShell.PPanels.PFunnelRules

[input] UI event <input>
- Receive the click event from the remove button @ owner.PFunnelActionBuild
> route

[route] Registered handler path <process>
- Remove funnel rule @ owner.PFunnelRuleRemove
- Apply funnel order @ owner.PFunnelOrderApply
- Update funnel empty @ owner.PFunnelEmptyUpdate
- Evaluate the handler's implemented conditional branches @ owner.PFunnelRuleRemove
- Update the visual or selection state owned by the handler @ owner.PFunnelRuleRemove
> output

[output] UI state after the event <output>
= The click event has completed through the current implementation.
! Code binding: cs|PPanels/PFunnelRules.cs|PFunnelActionBuild|pRemoveButton|Click|1
