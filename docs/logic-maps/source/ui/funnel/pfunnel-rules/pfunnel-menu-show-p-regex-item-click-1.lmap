@format 1
@id ui-event.45b98d1e04b09abc
@title Regex item — click
@section I. UI
@area UI event
@tabs Funnel
@event-ref cs|PPanels/PFunnelRules.cs|PFunnelMenuShow|pRegexItem|Click|1
@entry input
@summary Handle the click event exposed by PFunnelRules.PFunnelMenuShow and follow its registered code path.
@tag click
@tag PFunnelRules
@alias owner = Cadroue.UIShell.PPanels.PFunnelRules

[input] UI event <input>
- Receive the click event from the regex item @ owner.PFunnelMenuShow
> route

[route] Registered handler path <process>
- Add funnel rule @ owner.PFunnelRuleAdd
- Handle funnel press @ owner.PFunnelPressHandle
- Handle funnel move @ owner.PFunnelMoveHandle
- Handle funnel up @ owner.PFunnelUpHandle
- Apply funnel order @ owner.PFunnelOrderApply
- Update funnel empty @ owner.PFunnelEmptyUpdate
- Update the visual or selection state owned by the handler @ owner.PFunnelRuleAdd
> output

[output] UI state after the event <output>
= The click event has completed through the current implementation.
! Code binding: cs|PPanels/PFunnelRules.cs|PFunnelMenuShow|pRegexItem|Click|1
