@format 1
@id ui-event.9fab56db09485a47
@title Row — funnel row remove
@section I. UI
@area UI event
@tabs Funnel
@event-ref cs|PPanels/PFunnelRules.cs|PFunnelRuleAdd|pRow|PFunnelRowRemove|1
@entry input
@summary Handle the funnel row remove event emitted by Row through the exact route registered in PFunnelRules.PFunnelRuleAdd.
@tag funnel row remove
@tag PFunnelRules
@alias owner = Cadroue.UIShell.PPanels.PFunnelRules

[input] UI event <input>
- Receive the funnel row remove event from Row through its current registration @ owner.PFunnelRuleAdd
> route

[route] Registered handler path <process>
- Invoke the registered owner handler @ owner.PFunnelRuleRemove
! Registered call path: PFunnelRuleRemove
> output

[output] UI state after the event <output>
= The funnel row remove event has completed through the current implementation.
! Code binding: cs|PPanels/PFunnelRules.cs|PFunnelRuleAdd|pRow|PFunnelRowRemove|1
