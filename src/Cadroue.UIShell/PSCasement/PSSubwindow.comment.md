# PSSubwindow.cs

## `private const int PSSubwindowCaption = 0x00F7E8DC`

Deliberately duplicated, not shared with the tabless Dialog path.
The caption (DWM 0x00BBGGRR) is `PSSubwindowBackground` in BGR form.
Both are meant to match `PSHeadline`'s blue window.
