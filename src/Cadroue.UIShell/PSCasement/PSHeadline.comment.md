# PSHeadline.cs

## `internal const int PSHeadlineCaptionBlue = 0x00F7E8DC`

Kept separate from `PSSubwindow`'s caption on purpose (tabless vs tabbed).
The two blue captions are meant to match, so change both together.
