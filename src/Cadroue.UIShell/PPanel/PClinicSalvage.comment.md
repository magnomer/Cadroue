# PClinicSalvage.cs

## `public void PClinicSalvageUpdate(bool pClinicSalvageHasRepair)`

The source choice is always available.
The repaired-result choice only has meaning when at least one repair step is selected.
So disable that option alone when none is.
