# PInspectorWhitebalance.cs

## `private void PWhitebalanceManualUpdate()`

Method and Saturation stay live in every mode.
Custom is just the combo's face for a manual pick.
So this only keeps the combo selection and manual flag aligned.
