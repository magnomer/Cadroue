<Window x:Class="Cadroue.UIShell.PMainWindow.PWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:pControlBar="clr-namespace:Cadroue.UIShell.PControlBar"
        xmlns:pMainArea="clr-namespace:Cadroue.UIShell.PMainArea"
        Icon="/PAssets/PProgram/PProgramIcon.ico"
        Width="1280"
        Height="720"
        MinWidth="900"
        MinHeight="560"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        ResizeMode="NoResize"
        Background="#DCE8F7"
        BorderThickness="0"
        SnapsToDevicePixels="True"
        UseLayoutRounding="True"
        AllowDrop="True">
    <Grid Background="#DCE8F7" Margin="0" AllowDrop="True">
        <Grid.ColumnDefinitions>
            <ColumnDefinition x:Name="pTabRailColumn" Width="0" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="56" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <pControlBar:PToolbar x:Name="pControlBar" Grid.Row="0" Grid.ColumnSpan="2" />
        <ContentControl x:Name="pTabRailHost" Grid.Row="1" Grid.RowSpan="2" Grid.Column="0" Background="#EAF2FC"
                        HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch" />
        <pMainArea:PConsole x:Name="pConsole" Grid.Row="1" Grid.Column="1" Margin="0,-1,0,0" />
        <pMainArea:PDeck x:Name="pDeck" Grid.Row="2" Grid.Column="1" />

    </Grid>
</Window>
