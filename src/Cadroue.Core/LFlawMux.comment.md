# LFlawMux.cs

## `private static readonly string[] lFlawPipeline`

Complaints the throwaway null muxer of the -c copy -f null probe raises about its own output stage.
They describe our pipeline, not the input container, and follow from a codec-configuration defect the config detector already owns.
