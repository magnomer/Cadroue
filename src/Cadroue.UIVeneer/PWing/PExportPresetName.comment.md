# PExportPresetName.cs

## `private static TextBlock PExportTextBuild(string lPresetName, bool pPresetModified, bool pPresetUnsupported) => new()`

The incompatibility marker is display text only.
The preset keeps its real name, so renaming, matching and persistence never see it.
